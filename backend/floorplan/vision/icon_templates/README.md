# Room icon templates

These are the curated golden furniture-symbol samples from
`origin/django` commit `eab1d00`, originally stored under
`Final-Project_Version3/room_recognition/.golden/`.

Bella uses them read-only to suggest a room name from symbols already drawn on
an uploaded floor plan. The production path does not retrain models, harvest
user uploads, or write new samples into this directory.
