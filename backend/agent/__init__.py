"""RoomPilot 家具選件與擺位紀律。

本套件由三個部分組成：
- ``knowledge``：選件與成組擺放共用的宣告式領域知識。
- ``select``：讓 LLM 只從各房候選白名單決定「選哪些件」。
- ``place``：安排主件、副件的擺放順序，並處理放不下時的換小或移除。

LLM 呼叫器與擺放函式都由呼叫端注入，因此本套件不碰網路，也不依賴
``backend.server``。此處只決定選品、順序與失敗修復策略；不計算家具
座標，任何幾何與座標結果都必須來自 :mod:`backend.engine`。
"""
