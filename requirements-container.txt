# RoomPilot 容器執行期依賴。
#
# 與 requirements.txt（團隊 Windows 開發機 baseline）刻意分開，不是重複，理由有二：
#
# 1. baseline 同時鎖 opencv-python 與 opencv-python-headless。前者在 slim 容器
#    缺 libGL.so.1，`import cv2` 當場崩。容器只裝 headless——它提供同一個 cv2
#    模組，baseline 註解擔心的「torch 生態拉 headless 進來蓋掉 cv2」在這裡不
#    成立，因為映像裡根本沒有第二份 opencv 可蓋。HoughLinesP 的 <5 鎖照樣有效。
#
# 2. selenium / webdriver-manager / tqdm / beautifulsoup4 / requests 是 Kai 的
#    型錄爬蟲與 AWS metadata 工具。backend/ 與 scripts/ 都沒有 import（2026-08-05
#    全域 grep 確認），selenium 還會在執行期找 chrome。容器不需要。
#
# 版本一律與 requirements.txt 對齊；改動時兩份必須一起改。

# Geometry and placement (Ancai)
shapely==2.1.2

# FastAPI, persistence, upload and render integration (Bella)
fastapi==0.140.0
uvicorn==0.51.0
pillow==12.3.0
ezdxf==1.4.4
python-multipart==0.0.32
httpx==0.28.1
PyJWT==2.12.1

# Floorplan recognition and spatial evaluation (Cody / Django / Ben)
numpy==2.5.1
opencv-python-headless==4.13.0.92
svgpathtools==1.7.1
rapidocr-onnxruntime==1.4.4

# Catalog metadata and PostgreSQL (Kai)
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.2.2

# torch==2.13.0 刻意不列在這裡：PyPI 的 linux wheel 會連帶拉進 nvidia CUDA
# runtime（多約 2.5GB，本專案只推論不訓練，用不到）。Dockerfile 另用官方
# CPU index 安裝。改版本時記得同步 Dockerfile。
#
# 家具 RAG（sentence-transformers、huggingface-hub、openai、anthropic）不列入：
# ROOMPILOT_RAG_ENABLED 預設 false，且模型權重必須放在 repository 外。要在容器
# 裡開 RAG 請另外建置一層並掛 ROOMPILOT_RAG_MODEL_CACHE。
#
# PaddleOCR（requirements-ocr.txt）不列入：體積大且平台相依，缺它比例尺回到
# 手拉，服務不中斷。
