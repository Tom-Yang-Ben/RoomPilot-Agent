.venv/
__pycache__/

# Claude Code 本機設定與工作流（每台機器自行維護，不進版控）
.claude/
*Zone.Identifier

# MCP 設定（含 API keys，不可提交）
.mcp.json
!.mcp.json.*.example

# 編譯後二進位（無原始碼，不進版控）
*.exe

# CubiCasa5k 外部程式庫與權重（infer_cubicasa.py 用；重建：git clone CubiCasa/CubiCasa5k + README 的 Google Drive 權重）
CubiCasa5k/
model_best_val_loss_var.pkl
model_finetuned_*.pkl

# MitUNet 外部程式庫與權重（infer_mitunet.py 用；重建：git clone aliasstudio/mitunet + LFS 權重。權重 CC-BY-NC 禁商用）
mitunet/

# CubiCasa5k 資料集（5.5GB，換機時手動搬運 zip 再解壓到 CubiCasa5k/data/）
cubicasa5k.zip

# 房型評分：資料集圖複本（來源在 CubiCasa5k/data/，不重複進版控）
eval_rooms/input/

# 語意快取預覽圖與評分疊圖（分鐘級可重生，只提交 npz 與 report）
cubicasa/room/*_cc.png
eval_rooms/chk/

# 微調資料打包產物（own_dataset/ 原始碼進版控，打包件與 zip 可重生）
finetune_data/
finetune_data.zip
cubicasa/room_ft/
