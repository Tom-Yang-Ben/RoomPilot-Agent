.venv/
__pycache__/

# Claude Code 本機設定與工作流（每台機器自行維護，不進版控）
.claude/
*Zone.Identifier

# MCP 設定（含 API keys，不可提交）
.mcp.json
!.mcp.json.*.example

# 編譯後二進位（無原始碼，不進版控）
*.exe

# CubiCasa5k 外部程式庫與權重（infer_cubicasa.py 用；重建：git clone CubiCasa/CubiCasa5k + README 的 Google Drive 權重）
CubiCasa5k/
model_best_val_loss_var.pkl

# MitUNet 外部程式庫與權重（infer_mitunet.py 用；重建：git clone aliasstudio/mitunet + LFS 權重。權重 CC-BY-NC 禁商用）
mitunet/

# CubiCasa5k 資料集（5.5GB，換機時手動搬運 zip 再解壓到 CubiCasa5k/data/）
cubicasa5k.zip
