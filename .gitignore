.venv/
__pycache__/

# Claude Code 本機設定與工作流（每台機器自行維護，不進版控）
.claude/
*Zone.Identifier

# MCP 設定（含 API keys，不可提交）
.mcp.json
!.mcp.json.*.example

# 編譯後二進位（無原始碼，不進版控）
*.exe



# 語意快取預覽圖（分鐘級可重生，只提交 npz 與 report）
cubicasa/room/*_cc.png

# 本機自管總目錄，不 push：訓練產物（打包件/微調模型/驗收快取）＋
# CubiCasa5k 程式庫與資料集＋官方權重 model_best_val_loss_var.pkl＋
# 門/符號模板庫 npz＋管線預覽輸出 chk/＋房型評分工作區 eval_rooms/
# 換機：整包 training.zip 搬運解壓即可
# （人工答案在 Identify_ans/ 進版控；語意快取 cubicasa/ npz 進版控供雙機同步）
training/
training.zip
cubicasa/room_ft/
# 微調權重留專案根本地使用（200M 超 GitHub 100MB 限制），換機用 Drive/隨身碟帶
/model_finetuned_*.pkl
/cubicasa5k.zip

# 工作暫存（漏門裁圖、未標房間標示圖等除錯產物）
temp/
