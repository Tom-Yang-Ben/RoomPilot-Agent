# 私密設定
.env

# Python、Node 與測試快取
.venv/
.venv_triposr/
.uv-cache/
.pytest_cache/
frontend/node_modules/
frontend/dist/
node_modules/
__pycache__/
*.pyc
tmp/
.tmp/
.runtime/
.claude/settings.local.json
.claude/scheduled_tasks.lock
web_fastapi/uploads/
web_fastapi/__pycache__/
Miniconda3-latest-Windows-x86_64.exe
# floorplan2dxf 單檔模式的疊圖輸出(在 cwd 下產生;批次模式輸出在 data/testdata/chk/)
/chk/

# 僅限本機的封存與清理暫存區
_local_archive_not_for_push/
*_local_archive_not_for_push*/

# Old experiments and reference repos, not required by the current FastAPI website
.agents/
external/
TripoSR/
test_3Dfurniture/

# Non-runtime generated/intermediate model artifacts
data/dataset/decoded_glb_cache/
data/dataset/ikea/
sf3d/inputs/
sf3d/models/
sf3d/official_repo/
sf3d/outputs_normalized/
sf3d/outputs_raw/
sf3d/scripts/
sf3d/README.md

# Old diagnostic pages not used by the current website flow
web_fastapi/static/compare.*
web_fastapi/static/model_probe.*

# docs/ 只保留團隊專案文件，一律納入版控。
# 個人筆記、復刻研究與過時狀態稿已於 2026-07-21 移出 repo 封存。
# 註:bella-test1 原本的 `docs/*` 白名單會誤忽略 01_專題進度/、04_契約與規格/、
# archive/、ai-harness/,合併時移除,改由本規則統一納管。

# 2026-07-22 重組:data/ 現為版控資料夾(data/dataset + data/testdata),不再整包忽略
# dataset/ 已於 2026-07-07 進版控(blob 早已在 origin 歷史中,git 去重後零額外成本)
downloaded-files/
舊的翻譯資料/
needed.md

# 個人或工具產生的本機設定與筆記不提交。
.codex/
.claude/
CLAUDE.md
PROJECT_CONTEXT.md
CODEX_PROJECT_RULES.md
LOCAL_*.md
PRIVATE_*.md
PERSONAL_*.md
待修.txt
*_LOCAL.md
*_PRIVATE.md
*_PERSONAL.md
*_local.md
*_private.md
*_personal.md

# AI 協作規則必須版本化(路徑已對齊 2026-07-22 重組後的佈局)。
!AGENTS.md
!CLAUDE.md
!backend/**/AGENTS.md
!frontend/AGENTS.md
!data/testdata/AGENTS.md
!tests/AGENTS.md

# 大型 3D 模型、原始下載與批次預覽只保留在物件儲存或本機。
# 已清洗、可在網站直接使用的 PBR 紋理必須提交版本控制。
*.glb
*.gltf
*.bin
*.ktx2
*.hdr
*.exr
backend/catalog/data/staging/
backend/catalog/data/quarantine/lighting/
backend/catalog/assets/
backend/server/static/lighting_assets/
backend/server/static/texture_cache/

# 正式 PBR 材質（含 KTX2 壓縮貼圖）是網站執行期資產，必須上傳。
!backend/server/static/pbr_assets/
!backend/server/static/pbr_assets/**

# 本機跑第 8 步留下的成果 PDF 與資料庫傾印檔:都是可重新產生的產物,
# 且 dump 有 50MB 以上,進版控會永久膨脹 repo。
roompilot-proposal-*.pdf
roompilot_db_dump.sql*

# macOS 系統殘留(入倉三規則:禁止提交)
.DS_Store
# 分割模型權重 93MB — 進 git 前組長決定
backend/floorplan/models/*.onnx
graphify-out/*
.mcp.json