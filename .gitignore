# 私密設定
.env

# Python、Node 與測試快取
.venv/
.uv-cache/
.pytest_cache/
.tmp/
__pycache__/
*.pyc
node_modules/
.runtime/

# 僅限本機的封存與清理暫存區
_local_archive_not_for_push/
*_local_archive_not_for_push*/

# 僅限本機的資料集與暫存輸出（體積以 GB 計，不進版控）
# 必須用開頭斜線錨定在倉庫根目錄：未錨定的 data/ 會連 backend/catalog/data/ 一起吞掉。
/tmp/
/temp/
/data/
/dataset/

# 文件目錄只提交 Markdown、契約與已核准的 moodboard 圖片。
docs/*
!docs/*.md
!docs/backlog/
!docs/backlog/*.md
!docs/contracts/
!docs/contracts/*.md
!docs/moodboard_assets/
!docs/moodboard_assets/**
!docs/owners/
!docs/owners/*.md
!docs/vibecoding/
!docs/vibecoding/*.md

# 個人或工具產生的本機設定與筆記不提交。
.codex/
.claude/
CLAUDE.md
PROJECT_CONTEXT.md
CODEX_PROJECT_RULES.md
LOCAL_*.md
PRIVATE_*.md
PERSONAL_*.md
*_LOCAL.md
*_PRIVATE.md
*_PERSONAL.md
*_local.md
*_private.md
*_personal.md
docs/MISSING_*_AUDIT_*.md

# AI 協作規則必須版本化。
AGENTS.md
!AGENTS.md
!CLAUDE.md
!backend/**/AGENTS.md
!frontend3d/AGENTS.md
!scripts/AGENTS.md
!testdata/AGENTS.md
!tests/AGENTS.md

# 大型 3D 模型、原始下載與批次預覽只保留在物件儲存或本機。
# 已清洗、可在網站直接使用的 PBR 紋理必須提交版本控制。
*.glb
*.gltf
*.bin
*.ktx2
*.hdr
*.exr
backend/catalog/data/staging/
backend/catalog/data/quarantine/lighting/
backend/catalog/assets/
backend/server/static/lighting_assets/
backend/server/static/texture_cache/

# 正式 PBR 材質（含 KTX2 壓縮貼圖）是網站執行期資產，必須上傳。
!backend/server/static/pbr_assets/
!backend/server/static/pbr_assets/**

# 自 cody 分支併入的忽略規則（僅取安全與大型產物必要項目）。
# MCP 設定含 API key，是主線先前缺少的安全漏洞，優先補上。
.mcp.json
!.mcp.json.*.example

# Windows 下載來源標記檔，由瀏覽器自動產生，不屬於專案內容。
*Zone.Identifier

# 平面圖訓練材料體積以 GB 計，只留在本機；但研發工具與測試是程式碼，必須進版控。
# 負向規則不可省：辨識率的驗證鏈（評測腳本＋107 支測試＋報表）全在這幾個子目錄，
# 少了它們，cody-dev 宣稱的準確度數字在別台機器上無法重跑。
# 終版裁定（2026-08-03）：training/ 研發目錄不交付 ben，整目錄不進版控
training/
training.zip
/model_finetuned_*.pkl
/backend/floorplan/model_finetuned_*.pkl

# ── cody 管線側補充（2026-08-03 cody-dev 合併帶入）──
# 編譯後二進位（無原始碼，不進版控）
*.exe
# 工作流模板個人快取
VibeCoding_Workflow_Templates/
# CubiCasa 血統已移除，殘留壓縮包不進版控
/cubicasa5k.zip
# macOS Finder 中繼檔
.DS_Store
