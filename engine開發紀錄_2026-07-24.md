# Engine 開發紀錄 — 2026-07-24 開工版

> 個人開發筆記（07-25 起納入版控，推上 ancai 分支備份）。整理自 07-23 深夜與 Claude 的完整討論。
> 明早開工流程：先做「開工 Checklist」→ 回答「三題拍板」→ 照「實作順序」動工。

---

## 一、開工 Checklist（照順序做）

```bash
# 1. 確認位置與基準線
git branch --show-current        # 應在 ancai，乾淨
uv run pytest tests/ -q          # 基準：122 passed, 1 skipped（07-23 實測）

# 2. 開功能分支（還沒開過）
git switch -c feat/engine-room-profile

# 3. 回答下方「三題拍板」→ 開始實作順序 Step 1
```

**鐵則**：只在 `backend/engine/` 新增檔案＋`tests/` 新增測試。動到任何別人的檔案（`backend/server/`、`backend/agent/`、`backend/catalog/`、`frontend/`）之前必須先講。

---

## 二、三題拍板（開工前必答）

| #  | 問題                         | 選項                                                                                       |
| -- | ---------------------------- | ------------------------------------------------------------------------------------------ |
| Q1 | 沙發背落地窗是備案還是優先？ | A=實牆優先、放不下才背窗（保守）／B=偵測到「落地窗＋對面電視牆」就主動背窗（台灣實況）     |
| Q2 | 浴室／廁所預設家具？         | 留空（硬裝不做，只擺鏡櫃很怪）／最小組合（鏡櫃＋收納籃）                                   |
| Q3 | 堆疊做到哪層？               | 只做地面層（地毯墊底，小工程）／連桌面層（電視上櫃，需 2D 引入高度概念＋型錄沒有電視品項） |

---

## 三、已定案設計（不再討論，照做）

### 1. 落地窗留距（理想值／底線值雙層）

- 窗分三類：`一般窗`（維持現狀）／`固定落地窗`／`可通行落地窗門`
- 距離：固定落地窗 **理想 45 / 底線 30**；可通行 **理想 90 / 底線 60**（與現行門淨空 90 同標準；60=單人通行極限=逃生安全線，絕不再退）
- 引擎先試理想值，擺不下退底線，**退讓必須在結果標記**「已壓縮至最低通行距離」
- 使用情境：沙發**背對落地窗**留距（台灣客廳：電視牆在對面），不是把窗前劃禁區
- 落地窗 kind 沒人標之前，規則躺著不觸發（**不用寬度猜**）
- 豁免：地面層（地毯/門墊）與非落地物（壁掛/吸頂）

### 2. 房型白名單＋預設機能槽（清單式）

- **清單式白名單**：13 房型各自明列可用家具 `normalized_type`，**沒列到的一律不准**（廁所永遠選不到床）——取代現行「點名式」`ROOM_AFFINITY`（現況只限 7 樣，廁所可以放衣櫃書桌）
- 預設機能槽有序（主件先）。參考草案：
  - 客廳：沙發→茶几→電視櫃→地毯→單人椅→落地燈
  - 餐廳：餐桌→餐椅(×4)→餐邊櫃｜臥室：床→床頭櫃(×2)→衣櫃→五斗櫃
  - 書房：書桌→辦公椅→書櫃｜廚房：櫥櫃→推車｜玄關：鞋櫃→穿鞋凳→門墊
  - 走廊：長條地毯→壁架｜陽台：戶外座椅→戶外桌→植栽｜儲藏：層架→收納箱
  - 洗衣間：層架→推車（洗衣機 GLB 庫沒有）｜浴/廁：等 Q2｜其他：彈性
- 現況 13 房型只有 9 個有預設；浴室/廁所/走廊/其他全空

### 3. 堆疊分層

- Engine 現況**零堆疊概念**：`geometry.hits_furniture` 任何相交=違規
- 現行地毯是靠 `scene_service._IGNORE_COLLISION_TYPES` **完全跳過檢查**（偷吃步：也不擋門口淨空）
- 方向：疊放層（地面層/家具層/[桌面層看 Q3]），**同層才算撞**；地面層豁免所有淨空

### 4. 電視定位（幾何五因子，已雙重驗證）

評分因子（每名附理由，可解釋可重現）：

| 因子                       | 參數                                                        |
| -------------------------- | ----------------------------------------------------------- |
| 實牆連續長度               | ≥ 電視櫃 120 + 餘裕 20                                     |
| 本牆有窗（背光）           | −100                                                       |
| 對面有窗（反光，窗簾可解） | −30                                                        |
| 觀看距離（55 吋預設）      | 250–450cm +50；180–550 +20；其他 −50                     |
| 對面沙發＋走道             | 剩 ≥60cm +30，否則 −60                                    |
| 門淨空                     | 電視/沙發壓進 90cm 門淨空 → 該位置直接排除，沿牆滑動找替代 |

- 輸出：第 1~N 名電視牆＋座標＋沙發對面座標＋理由；或誠實回報「無合法電視位＋原因」
- **門段是必要輸入**（見下方證據 C）

---

## 四、實作順序（每步：先寫測試→實作→跑全套 pytest→驗收）

```
Step 1  房型白名單＋預設機能槽
        新檔 backend/engine/room_profile.py + tests/test_room_profile.py
        內容：13 房型白名單、預設槽、家具族系表(FAMILY_OF)、查詢 API
        （allowed_types / is_type_allowed / default_slots）
Step 2  堆疊分層
        改 backend/engine/models.py(加疊放層欄位) + geometry.py(同層才撞)
        ＋地面層豁免；新測試
Step 3  窗模型＋落地窗留距＋電視定位
        engine Room 加窗段+kind（現況 Room 只有牆、連窗都不存在）
        留距檢查（理想/底線+退讓標記）掛進 check_placement_with_clearance
        電視定位純函式（五因子，輸入房間多邊形+門段+窗段）
```

**設計原則**：新增參數都給預設值 → 現有呼叫端（scene_service 等）一行不改也不會壞；產品行為要等上游接手才變，engine 先做對＋測試證明。

---

## 五、實驗證據（這些都是實跑過的，不是推測）

**A. 全流程 API 實跑通過**（07-23）：floor21.dxf 走完七步（建案→上傳→辨識→校尺→空間確認→問卷→2D→白模→視角→色卡→PNG），12 個 revision，人工 gate 全部在型別層強制（`user_reviewed: Literal[True]`）。

**B. 校尺=全案匯率**：同一張 floor21 只改宣告牆寬（200~900cm），房間面積 2.99↔70.87 m²（差 24 倍），**引擎全程零抗議**（3 m² 房塞 42% 佔比的沙發也判合法）——比例錯是靜默失敗，可考慮在 engine 加合理性警示。

**C. 門資料會翻盤答案**（floor21 實證）：

- 上傳 **DXF → 門=0**（DXF 圖層設計只有 WALL/WINDOW）；上傳 **PNG → 門在交接 JSON**（floor21 有 2 扇，信心 0.59/0.556）
- 無門版最佳電視牆，加回 2 扇門後**整面被門淨空夾殺淘汰**（沙發位直接壓門）；新冠軍=北牆東段 電視(129,137) 沙發(129,-104) 視距 323
- 結論：**電視定位函式強制要求門段輸入**，缺門要標記「僅供參考」

**D. 21 圖掃盲測試**：16/21 成功給出電視位＋理由；5 張「失敗」全是誠實回報（6~12 m² 小房被 4~13 扇門窗打碎實牆，本來就不該硬塞）——寧缺勿亂正確運作。且輸入含明顯辨識誤判（floor01 有 17 扇門），乾淨資料下成功率更高。

**E. Engine 現況體檢**：

- `placement.py` = 房中央撒 13 點×4 角度的笨網格，是墊底方案
- 擺位智慧（靠牆/成組/背後探針/避窗 15cm）全在 `scene_service.py`（別人的，1727 行）
- engine 零房型概念、零窗概念、零堆疊概念——`type` 欄位只是不被讀的標籤
- 三份知識表現況位置：`layout_service.py:40 DEFAULT_TYPE_GROUPS`／`agent/knowledge.py COMPANION_OF+ROOM_AFFINITY`／`catalog/style_db.py:182 CLEARANCE_BY_TYPE`（只有 4 類淨空）

---

## 六、型錄字彙對齊注意（Step 1 會用到）

- 正字法用 catalog 的 `normalized_type`（kebab-case）。CSV 英文名→slug 陷阱：
  `Chest Of Drawers→chests-of-drawer`｜`Storage Boxes Baskets→storage-boxes-basket`｜
  `Childrens Stools Bench→childrens-stools-benche`(原始 typo)｜`Sheepskins Cowhides→sheepskins-cowhide`｜
  cabinet-cupboard 與 cabinets-cupboard **兩種拼法並存**
- **型錄沒有「電視」品項**（只有 tv-bench 49＋tv-media-furniture 11）；GLB 庫沒有洗衣機/冰箱/抽油煙機（runtime 型錄有少量）
- 庫存最大宗沒被用到：stool-bench 1393、地毯 1041、燈具 658——白名單記得納入
- GLB 庫獨有、runtime 型錄沒有：wall-lamp／ceiling-lamp／pendant-lamp（壁掛/吸頂，不佔地面）

## 七、之後要跟組員談的（不擋 engine 動工）

1. ~~落地窗 kind 標注 UI → 建議加在「空間確認」步（frontend）~~ **07-24 已解決**：前端已有 `scene_window_types.js`（standard／floor_to_ceiling 兩類），`window_type` 隨 structures.windows 流進後端 window_segments（segment() 保留額外欄位）。落差：只有兩類，缺「可通行落地窗門」→ 建議引擎先照兩類做，落地窗一律當固定式（保守值）
2. scene_service 接 engine 新規則：~~現行「背貼窗 15cm 就換牆」與沙發背窗新規**直接衝突**~~（**07-24 更正：全 repo 掃描查無此實作，此規則不存在、無衝突**）；`_IGNORE_COLLISION_TYPES` 偷吃步屆時退役（backend/server）
3. 電視定位接進擺位流程（現行沙發先擺、電視櫃找對面；新設計反過來）（backend/server）
4. Demo 注意：**上傳 DXF 路徑門永遠是 0** —— demo 用 PNG 或在空間確認手動補門

---

## 八、2026-07-24 當日總結（Claude session）

### A. 已完成（環境與 LLM 管線）

- **依賴修復**：`server` extra 不含 opencv 但 main.py 硬 import vision → `uv sync --extra server --extra vision` 一次裝齊
- **`.env` 整理**：兩個啟用開關改新名（`OPENROUTER_REQUIREMENTS_ENABLED→OPENROUTER_INTAKE_ENABLED`、`OPENROUTER_LAYOUT_ENABLED→OPENROUTER_SCENE_PLANNING_ENABLED`），刪殭屍變數 `OPENROUTER_VISION_MODELS`／`OPENROUTER_FLOORPLAN_VLM`；key 驗證有效（付費帳戶）
- **免費模型全滅事件**：原 6 個 `:free` 模型全被 OpenRouter 下架（404），LLM 靜默退回規則模式無人察覺 → 換成付費輪詢 `gemini-3.1-flash-lite → mistral-small-24b → seed-1.6-flash → deepseek-v3.2`（實測挑選）。問卷 21.7s→**2.0s** 且抽取全對（含貓、床不對門）；單次呼叫 <$0.001
- **已知 bug 未修（待拍板）**：`scene_service._normalize_openrouter_plan` 不驗 `layout_rules` 項目型別，LLM 回字串陣列時 `:1781` `rule.get("message")` 炸 500——gemini 3 次踩 2 次，已成場景生成主要故障源。修法一行（字串包成 `{"message": str}`），檔案在 backend/server/
- 觀察：兩處 LLM 呼叫錯誤全被 `except: continue` 靜默吞掉、per-model timeout 硬編 8 秒偏緊

### B. LLM/Agent 功能盤點（workflow 掃描）

- 全 repo 真正呼叫 LLM 僅 2 處：intake 抽取＋scene plan（皆已通電並實測）
- **Yen 選件=鋪好沒通電的軌道**：prompt／白名單驗證／雙層 fallback／32 測試齊全，唯 `complete` 沒接真呼叫器（main.py:2215 是回傳 payload 的 lambda），前端不帶 `llm_selection` → 實務 100% 走 local_rules
- **scene.js 是死前端**：無 HTML 載入（scene.html 只載 scene_v2），問卷 LLM 目前只有 API、無活的使用者入口
- `.env.example` 缺兩個 enable 開關（照範本設定 LLM 永不啟用）
- `engine/schema.py` 預留 `PLACE_FURNITURE_TOOL`／`ADJUST_FURNITURE_TOOL`（未用）＝未來擺位 Agent 的 function-calling 介面

### C. 型錄盤點（資料基礎確認齊備）

- 桌面 CSV（19 大／64 小分類）＝9,350 件 GLB＝manifest 筆數完全吻合，**全數上 S3/CloudFront**（07-24 上傳完成）
- 六風格型錄 10,550 筆／86 type；`family_of` 覆蓋率 **100%**（兩份全比對零漏網）
- 型錄比 GLB 多 ~1,200 筆（家電／戶外／壁畫，無 3D 模型）
- `must_against_wall` 全 False、`can_rotate` 全 True——兩個死欄位從沒人填

### D. 引擎規則缺口分析（workflow 掃描；完整清單存 `.claude/context/decisions/`）

五大類缺口（現行引擎=「不違規」，尚無「住得舒服」）：

1. **人因淨距**：走道最小寬（任兩件家具可零距貼齊！）、床側下床淨距、沙發-茶几 35-45、餐椅拉出 75-90、開合淨空只有 4 類（抽屜櫃 225 件裸奔）
2. **門窗語意**：Room 無門窗欄位；問卷抽得出「床不對門」但引擎做不到（契約斷層）
3. **高度維度**：height 資料 98.8% 齊、引擎 0% 使用（衣櫃vs樓高、窗台90vs窗前櫃、吊燈淨高）
4. **堆疊層**：檯面物 1,000+／壁掛 250+／地毯 1,041 件無處放或亂放；wall-shelf 寫死左上角同房兩件完全重疊
5. **構圖**：床頭櫃成對、書桌朝向（辦公椅寫死假設桌在上牆）、群組軸線對齊

建議優先序：走道+間距 → 堆疊層表 → 門窗語意 → 開合淨空擴充 → 高度/構圖

### E. 架構方向（討論定調，未動工）

- **RAG+Agent 流程圖對齊**：Vector DB 全新；複合式 RAG 插座＝候選白名單生成處；配套 Agent＝Yen 通電（~50 行呼叫器）；擺位 Agent＝function-calling 指揮引擎。**LLM 不碰座標鐵律不變**
- **API**：全部吃現有 OpenRouter key（embeddings 端點實測也通），無需新帳號；擺位 Agent 需支援 tool-calling 的模型（現輪詢清單皆支援）
- **6+7 步合併提案**：白模當主舞台＋2D 小地圖（**保留可編輯**，平面圖是排版精度最高介面）；白模功能全數保留；引擎零改動（白模本來就吃 2D 引擎座標，3D 拖曳吸牆已走 /api/scene/layout）。前端組員地盤，可與 Agent 工作平行
- **不重寫 3D 引擎**：走 2.5D（家具加 `layer`＋`elevation_cm`，同層才碰撞）＝總帳堆疊層的實作；理由：問題本質是平面、collision_box 僅 29% vs footprint+height 98.8%、122 測試資產不可拋
- **家具擺放對照表**：建 86 型 type-level 表（放 backend/engine/ 新檔）而非逐筆改 10,550 筆；附 CI 哨兵測試（型錄任何 type 不在表裡就紅燈）；`must_against_wall` 一併修

### F. 待拍板彙總（下次開工先答）

1. layout_rules bug 修不修（backend/server/，一行，場景成功率 1/3→3/3）
2. 對照表四題：五層（加壁掛/天花）？籠統 type 保守預設？家電 20 型納入？盆栽高度 >50cm 判落地？
3. 缺口清單（D 節五類）圈選哪些先做
4. `.env.example` 補兩個開關
5. TaskMaster hook stdout 警告修不修／整組 hooks 去留
6. 原「三題拍板」（§二 Q1-Q3）仍未答；Q3 堆疊層級與對照表四題可合併決策
