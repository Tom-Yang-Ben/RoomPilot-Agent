# Codex 專案指令

開始處理這個專案前，請先讀取並遵守：

```text
PROJECT_CONTEXT.md
```

歷史基準來源：

```text
D:/產業新兵計畫/期末專題/test_furniture/PROJECT_CONTEXT.md
```

說明：

1. 目前 repo 內的 `PROJECT_CONTEXT.md` 是現行工作上下文。
2. `D:/產業新兵計畫/期末專題/test_furniture/PROJECT_CONTEXT.md` 視為歷史版本與沿革參考。
3. 若現行檔案與歷史內容有落差，先以現行需求與使用者最新指示為準，再視需要回補歷史脈絡。

特別重要：

1. 回答或修改程式前，先理解 `PROJECT_CONTEXT.md` 的專題目標、目前決策與使用者教學規則。
2. 使用者要求教學時，要先說明「接下來要做什麼、為什麼、做完會得到什麼」，等使用者說「好」後再給指令或程式碼。
3. UI 需要中文化，並清楚標示 XYZ 正負方向。
4. 如果修改了專題方向、重要功能、環境或檔案，請同步更新 `PROJECT_CONTEXT.md`。
5. 不要把 TripoSR 當成已確認可滿足精緻家具需求的主線；目前它只是已嘗試過且品質不足的方案。
6. 當聊天室或上下文狀態接近 80% 時，請提醒使用者壓縮內文或更新 `PROJECT_CONTEXT.md`，避免後續遺失重要進度。
7. 當 `PROJECT_CONTEXT.md` 變得過於繁瑣或難以快速接手時，請提醒使用者拆成 `docs/` 子文件，不要無限制塞進同一份交接檔。

## 分支準則

1. 只在 `bella` 分支上工作，不主動切換、修改、提交或整理其他分支。
2. 開始動手前，先確認目前分支是 `bella`；如果不是，先停止修改並提醒使用者。
3. 若需求涉及 `main`、`ben` 或其他分支，只能提供說明、比較或建議，不直接變更那些分支的內容。
4. 若需要跨分支比對，僅限唯讀檢查；未經使用者明確要求，不進行 `merge`、`rebase`、`cherry-pick`、`reset`。
5. 若發現工作目錄內容看起來不像 `bella` 的預期狀態，要先告知使用者再繼續。

## 建議使用方式

1. 每次新開對話或新接手任務時，先讀 `PROJECT_CONTEXT.md`，再讀這份檔案。
2. 需要追溯舊決策、早期 UI、歷史限制或前一版方向時，再回讀歷史基準檔 `D:/產業新兵計畫/期末專題/test_furniture/PROJECT_CONTEXT.md`。
3. 如果本次任務有改到流程、檔案位置、啟動方式、主要 UI、資料契約或專題方向，完成後同步更新 `PROJECT_CONTEXT.md`。
4. 如果需求和這份規則衝突，以使用者當前明確要求為優先，但要先提醒風險與影響。
