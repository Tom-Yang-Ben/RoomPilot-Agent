# Color 管線調優計劃書

- **日期**: 2026-08-01
- **前置**: color 答案集 28 張已全數人工審定（own_dataset_color 19 張 152 間、
  own_eval_color 9 張 62 間，0 Undefined，parse_gt 全過）。
  重複已清：color_floor_11（同 floor_04）、灰階 floor19（與保留集 floor60 洩漏）。
- **相關**: 灰階調優歷程與失因分析見 `SEG_FAILURE_ANALYSIS.md`（v2.23/v2.24）。

## 目標

1. **Phase 0 — Baseline**：用乾淨 GT 對 color 集首次量測，建立基準數字。
2. **Phase 1 — Color 調優**：以 own_dataset_color 為開發集做失因歸類與修正，
   目標比照灰階：整體命中 ≥ 80%、核心五類（Kitchen/LivingRoom/Bedroom/
   Storage/Bath）命中 ≥ 85%。
3. **Phase 2 — 墨水層殘餘**（color 告一段落後）：
   - floor_02：圍籬線進 thin 層造成的切割問題
   - floor_13、floor_09：零符號圖的命名證據不足問題

## 量測方式（Phase 0）

```
# 開發集（19 張）
python training/scripts/eval_rooms_cc.py \
    --own-dir testdata/Identify_ans/own_dataset_color --report-suffix baseline

# 保留集（9 張）——只在循環收尾跑，平時不看
python training/scripts/eval_rooms_cc.py \
    --own-dir testdata/Identify_ans/own_eval_color --report-suffix baseline
```

- 報表落在 `temp/json/report_own_own_dataset_color_baseline.json`（保留集同理）。
- 指標：hit_rate（IoU≥0.5 配對率）、overseg、mean_iou、per-class P/R、
  seg_fail 數。核心五類命中另行從 confusion 算。

## 紀律（沿用灰階循環的鐵律）

- **開發只看 own_dataset_color**；own_eval_color 每循環收尾量一次，防過擬合。
- 疑似答案卷標錯 → 帶證據（疊圖）找使用者一起看，不硬調管線遷就。
- 房間切割一律軸對齊直角矩形系（0/90/180/270）。
- 改 `testdata/Identify_ans/` 任何檔案前先問使用者、先備份。
- 每個假設先寫測試（TDD），失敗假設誠實記錄並 revert。

## 待驗證進度（checklist）

### Phase 0 — Baseline
- [x] own_dataset_color 19 張跑通（18 ok＋1 seg_fail，無 error/svg_mismatch）
- [x] own_eval_color 9 張跑通（9 ok）
- [x] 基準數字記入本文件「Baseline 結果」節
- [x] 失因初步歸類（seg 層 vs 命名層 vs 偵測層），加權重排序（見
      「失因加權歸類」節）

### Phase 1 — Color 調優（依 baseline 失因決定攻擊順序，逐輪補記）
- [x] 第一輪：**分割黏連/漏切**——dev 84→92 命中（60.5%）、seg_fail 歸零、
      零退化。三個機制（皆 TDD）：
      1. `hollow_wall_rects`：空心雙線牆（細深描邊夾白填充）補抓；
      2. `color_window_layers`＋detect_color 接回窗封口（floor_09 型的
         窗帶灌水漏）；thin 只以 `det["fence"]` 供救援輪，不當墨水證據
         （彩圖細線滿是磁磚/家具線，會壓制走道橫斷合併）；
      3. `window_side_gate`：假窗物理判別（真窗恰一側通室外；屏障加
         1.5T 閉運算補殼縫、探針跳閉運算帶）。
      成績：floor_07 seg_fail→4/10、floor_20 2→4、floor_01/03 各 +1。
      **殘留**：floor_08 內牆含「近白暗示牆」（描邊 gray 189~200，灰度
      不可偵測，需語意或色塊分割路線）；floor_09 仍 1/7（窗封口不夠，
      漏點另查）；floor_07 剩 6 間未中。
      **教訓**：eval 暫存檔名含 color 會強制彩圖路（2x），手動診斷須用
      staged 檔重現，用原檔名會走灰階路得到不同結果。
- [x] 第二輪：**Balcony 全滅→大致解決**——dev 92→97（63.8%）、配對房
      命名 95.9%、零退化。兩個機制：
      1. **DINOv2 分域雙頭**：叫錯的根因是灰階頭沒看過彩圖（磚紋認成
         樓梯 Stair×5）。color 答案集餵進訓練；混訓實測傷灰階
         （86.1%→80.6%），改 `--domain` 分域：灰階頭原封不動、color 頭
         83.9% 出貨為 `room_head_color.npz`，依 `det["fence"]` 選頭。
         注意：灰階基準已從 90.3% 移到 86.1%（答案卷標籤修正所致，
         新舊頭同分、非退化）。
      2. **陽台口袋收割**（`_harvest_balcony_pockets`）：圍欄補屏障重
         灌水收殼外封閉口袋，閉運算防圍欄線切碎。Balcony 配對 5→16。
      第三輪原列的 Bath/Kitchen 命名已被雙頭一併解決（Bath R 1.0）。
      **殘留**：Balcony 漏切 13 間——圍欄未進細線層（floor_15 型，
      洗衣工作陽台的邊界不是細線而是家具/器材輪廓）。
- [x] 循環一收尾（2026-08-02）：holdout 量測（cycle1b）——
      | 指標 | baseline | 循環一後 |
      | :--- | :--- | :--- |
      | dev 命中 | 59.2%（84/142，1 seg_fail） | **63.8%（97/152，0 seg_fail）** |
      | dev 配對房命名 | — | **95.9%**（核心五類 96.0%） |
      | holdout 命中 | 67.7%（42/62） | 66.1%（41/62，floor_27 -1） |
      | holdout 配對房命名 | 62%（26/42） | **90%（37/41）** |
      | holdout 核心五類 | 72% | **94%** |
      | **端到端全對（切對＋叫對）** | holdout 26 間 | **37 間（+42%）** |
      過程中抓掉一個保留集才暴露的雷：fence 救援輪在深磁磚圖拼碎房
      （floor_30 5→1），加紮實度驗收（<0.60 讓路）後復原。
- [x] 第三輪（2026-08-02）：floor_09 型漏水診斷落地——
      1. `envelope_gap_seals`：外圈 260~600cm 封口（窗帶寬過常規上限、
         窗偵測 R 38% 擋不住的最後防線）。dev 持平、過切 1.29→1.26，
         已啟用。
      2. 長牆豁免 `WALL_RESCUE_LONG`（**預設關**）：木紋真牆與家具的
         可分維度是長度（家具 <5T、真牆 ≥8T），實測救回 7 段讓
         floor_09 全戶圈住（segment 6→13 間），但**衣櫃長邊同為長細
         深木紋被誤救**、室內切線帶歪，dev 淨 ±0。行為契約已測試固
         定；下輪候選判別：候選兩端是否接牆網。
      floor_09 殘餘：房間已存在但邊界由家具帶定義，IoU 配不上 GT。
- [x] 循環二收尾（2026-08-02，cycle2）：陽台口袋條件鬆綁後——
      | 指標 | baseline | 循環一 | 循環二 |
      | :--- | :--- | :--- | :--- |
      | dev 命中 | 59.2% | 63.8% | **67.8%（103/152）** |
      | holdout 命中 | 67.7% | 66.1% | **69.3%（43/62）** |
      | holdout 命名 | 62% | 90% | **91%** |
      | holdout 端到端全對 | 26 | 37 | **39（+50% vs baseline）** |
      dev Balcony 配對 19 間全對（循環前端到端 0/28）。
- [ ] 達標判定：dev 命中 ≥ 80% 且核心五類 ≥ 85%——**命名側已過**
      （dev 95%/holdout 94%），**切割命中未達**（dev 75.0%，差 8 間），
      殘留：floor_07/08/09 棄守畫風 12 間（07 剩 4：Kitchen 0.39/
      Bath2 0.24/Bed 0.44/Hallway；08 剩 4：Bed2 0.24/Bath 0.19/
      Kitchen 0.35/Hallway；09 剩 5）、floor_10 貪婪配對邊際 -1、
      散件（Hallway/Entry 小間）
- [ ] Readme changelog 補版本節（v2.25）

### Phase 2 — 墨水層殘餘（灰階，2026-08-02 收尾）
- [x] 灰階開發集重量測（分母 24 張/153 房）：**83.7%（128/153）、核心
      五類 96.3%、seg_fail 0**——color 循環的口袋收割等機制在灰階同步
      生效，舊 82.2%/157 基準退役。
- [x] own_eval 保留集收尾：**59/72（81.9%，比 v2.24 的 58/72 再 +1）**，
      命名 84.7%。
- [x] floor38/44 浴具案：已在前循環修復（不在漏切清單）。floor_02 現
      4/6，圍籬救援輪運作中，殘餘 2 間屬其他失因。
- [x] 開放 LDK 切分機制落地（分數持平、結構就位）：切分守門 DINO 讓行
      鉤子＋符號子群離群剔除。**零轉化根因＝符號偵測召回**（floor47/52
      真廚房只偵測到單一符號、floor13 整圖零符號）——瓶頸移交偵測層。
- [x] 符號召回攻堅（2026-08-02，**實測死路、零出貨**）：floor13/09 零
      符號的三條路全走過——
      1. 門檻放寬：floor13 近失候選（ch 1.0~1.09）全是假陽性（床→chair、
         管道間→chair），放寬＝毒藥；真浴具（12px 馬桶）連尺寸閘門都
         過不了。
      2. thin 層 NN/cubic 上採樣：零效——1x 萃取時細節已喪失。
      3. 源頭 2x 重萃取（cubic 放大原圖重建細線層）：有召回但全是錯種
         假陽性（馬桶/盆栽→ksink），會給浴室灌廚房分數。
      **結論：瓶頸在模板庫缺美式極簡畫風的衛浴/廚具符號**，工程手段
      （門檻/解析度）不可解。可行路徑＝依 v2.24 的
      probe_symbol_quality 量測驅動流程，從該畫風補模板進 Asset 庫再
      外科啟用——屬素材整理任務，另立一輪。
- [ ] （移交下期）該畫風模板補充（素材任務）；床頭板/櫃緣長線與真牆的
      判別（WALL_RESCUE_LONG 啟用前置）

## 下期路線圖（2026-08-02 收盤時狀態）

四條線，各自獨立成輪：

1. **門偵測重建**（灰 P 37.8/R 17.1 遠低於可用線）——**方向已實證
   （2026-08-02）**：build_rooms 的 zones（門尺寸牆縫＋墨水證據，黃
   框）當門候選，前 10 張實測 **P 0.86／R 0.75**（38/44、38/51），
   弧掃描全盲的 floor02/04 直接 5/6、4/4。下一步＝把產品 doors 輸出
   接到 zones（floorplan2dxf 批次 json 的 doors 欄位改源）＋殘餘
   13 樘漏門逐案；彩門同機制可直接受益（zones 彩圖也有）。
2. **彩窗專輪**（P 63/R 40）：微調已證死路（0.60 墨跡上限實測
   R+2/P-4 還原）。已定位的結構缺口：(a) 牆角塊不在共線分群、開口
   靠 scan_extra 虛擬夾邊補——floor_28 案已枚舉但 covered_soft 三
   重門檻（0.45 墨跡/2 貫穿群/edge_hug）對彩色線寬不適配；需按畫
   風分開校準覆蓋結構。fp_c.detect_windows 彩圖專用、灰階走 fp_bw
   不受影響。
3. **Stair 約束層**（灰 3 間）——**2026-08-02 診斷收案，維持不動**：
   floor13 實測樓梯只有 3 條粗踏板（間距 45/31 不等距），
   MIN_TREADS=4 與等距約束雙雙不過；放寬到 3 條會撞上已文件化的
   「衣櫃分隔線幾何同構不可分辨」風險。結構性風險區，EV 3 間不值
   得冒 FP 險。
4. **大工程線**：色塊分割（攻彩棄守 17 間，天花板最高）；美式畫風
   模板素材（攻灰符號召回，需使用者參與裁切）。

## 彩色專攻期紀錄（2026-08-02 起，灰階凍結）

- **彩門證據層收案（零出貨）**：四判別器全數實測不可分——迴轉區
  fence 密度（TP 0.63/FP 0.59）、門弧近鄰（98%/96%，假弧遍地）、
  幾何錨定弧（51%/24%，當硬閘 R 腰斬）、門扇白淨度（TP P25 僅
  0.05）。彩門現況 P 0.38/R 0.47；突破繫於白牆萃取（見下）成功後
  縫品質整體提升。
- **色塊分割線戰況（2026-08-02 下午）**——已出貨鏈：
  `white_wall_rects`（白帶 top-hat＋方向性長核＋帶級側翼＋暗牆網對
  齊＋原始 chroma 白閘）→ 白牆救援（災難張雙型觸發：低覆蓋房少/
  巨塊黏連；採用線 cov≥55%；重跑帶 seal_hi=360）→ 救援採用張提案
  強化（任意對 0.7、10m² 門檻、8 色相桶色染刀、8:1 比例守門）。
  **floor_08 棄守首間翻盤（1→2/7、救援採用 73%→66%/3→6 房）**，
  彩 dev 111/152（73.0%）。
  下輪接點：(a) floor_08 propose 已開兩刀（插樁證實 8→10 房）但切
  界與 GT 的 IoU 未跨線——逐 GT 邊界精修（切點步進對齊/色界細
  化）；(b) floor_09 救援覆蓋 48~49% 差 55% 線一步（假帶再降或真
  牆再撈）；(c) floor_07 未進入戰場。
- **接點 (b) floor_09 診斷收案（2026-08-02 晚，維持不動）**：逐 GT
  量測推翻「差 55% 一步」假設——救援輪六 GT 房都在房內
  （0.65~0.96）但被假帶碎切，最大單房佔比僅 0.15~0.34，跨線也配
  不上；且救援輪把主分割已配上的 Balcony（0.528）灌到室外 0.77，
  採用＝倒賠。假帶判別三特徵（兩側同色相桶/平行近鄰/端接暗牆網）
  逐帶標定後全數五五開、無可分維度——帶級過濾在此畫風已到原理
  極限，與棄守裁定一致。55% 閘門保持原樣（它正確地擋住了退化）。
- **接點 (a) floor_08 假帶縫合輪出貨（2026-08-02 晚）**：pred×GT
  重疊矩陣定位出「過切殘塊獨立成房」型失分——客廳被切出 21k px
  殘片（100% 在 GT LR 內、被叫 room/Hallway）。新機制
  `_ww_suture_merge`（TDD 7 測試）：救援採用張限定，四道門
  （災難張閘/邊界非真暗牆 <0.3/一方殘塊名/DINO 聯集重分類
  ≥0.7 同主體）全過才併，greedy 取聯集信心最高、上限 4 併。
  實測殘塊多為 fence 切非白帶切（邊界 dark 0.03），門 2 只擋暗牆。
  floor_08 三殘片併回客廳，2/7→3/7（LR IoU 0.38→0.539 跨線）。
  dev 全集 A/B（suture1 vs tk6）：**112/152（73.7%）**、過切
  1.40→1.38、其餘 18 張零變動。下輪接點：floor_08 Bed2 需
  「切臂＋併」兩步（#9 的 Bed2 臂 20% 切出併 #3 可達 ~0.89）、
  Kitchen 在 Bath blob 開浴/廚刀（~0.55 邊際）；floor_07 未進戰場。
- **floor_07 攻堅：碎裂型災難三件套（2026-08-02 晚，dev 75.0%）**：
  floor_07 主分割 cov 42%/58 房，「低覆蓋房少」「巨塊」兩觸發都不
  中——第三型災難。三機制（皆 TDD，propose 測試 10 個）：
  1. **碎裂型觸發**：cov<50% 且主分割房數 ≥20 進救援輪；
  2. **收斂採用閘**：碎裂型救援必須房數收斂（floor_07 58→13 ✓；
     floor_04 22→41 越補越碎＝白帶在正常張灑假牆，覆蓋過閘也拒收
     ——初版誤火 9/10→6/10 的教訓）；
  3. **證據刀限定**：災難張 5~10m² 帶（門檻下修：硬地板 8→5m²、
     big 線 10→6m²，比例尺低估的複合 blob 才進得了場）禁中點亂刀，
     只吃 tint/fence/剖面跳變證據刀（floor_08 左臥 7.2m² 被中點對半
     誤切 -1 的教訓）。
  floor_07 4→6/10（LR 0.880、Balcony 0.693、Bath 0.688 轉化；縫合輪
  在此張立功 4 併）。**放棄的殘留**：id 3 臥+浴 blob 的第 7 間配對
  依賴中點刀（與 floor_08 誤切零和）；判別維度三連敗——帶級特徵
  五五開、整房 DINO 信心反向（blob 0.9+/乾淨房 <0.8）、bbox 填充率
  反向（blob 0.91~0.98 方正/L 臥 0.61），誠實收案不再追。
- [x] 循環三收尾（2026-08-02，shatter2/cycle3）：
  | 指標 | baseline | 循環一 | 循環二 | 循環三 |
  | :--- | :--- | :--- | :--- | :--- |
  | dev 命中 | 59.2% | 63.8% | 67.8% | **75.0%（114/152）** |
  | dev 配對房命名 | — | 95.9% | — | **94.7%**（核心五類 95.2%） |
  | holdout 命中 | 67.7% | 66.1% | 69.3% | **69.3%（43/62）持平** |
  | holdout 命名 | 62% | 90% | 91% | **91%** |
  | holdout 端到端全對 | 26 | 37 | 39 | **39** |
  holdout 過切 1.66→1.56（floor_28 -1/floor_29 +1 互換）。災難張
  機制對 holdout 淨零——保留集無 07/08 型棄守畫風災難張，符合
  「災難張限定、正常張零接觸」的設計承諾。

## 棄守裁定（2026-08-02 使用者裁定）

**color_floor_07/08/09 棄守**（dev 殘餘 18 間、佔 37%）：近白暗示牆/
空心雙線/木紋牆誤刪等畫風問題，「用暗色找牆」古典路線原理極限。
仍留在分母如實計分，但不再投工程；火力集中在灰階＋彩圖通用的切割
提升（DINO 提案式切分、Balcony 殘餘、Bath 小間）。

## Baseline 結果（2026-08-01）

| 指標 | 開發集（19 張） | 保留集（9 張） |
| :--- | :--- | :--- |
| 命中率（IoU≥0.5） | **59.2%**（84/142） | **67.7%**（42/62） |
| 核心五類命中 | **56.7%**（38/67） | **72.2%**（26/36） |
| 過切率 | 1.08 | 1.39 |
| 配對平均 IoU | 0.857 | 0.789 |
| seg_fail | 1（floor_07） | 0 |

報表：`temp/json/eval_rooms/report_own_own_{dataset,eval}_color_baseline.json`

### 逐張命中（開發集）

| 張 | 命中 | 張 | 命中 |
| :- | :--- | :- | :--- |
| 01 | 4/7 | 12 | 10/12 |
| 02 | 5/9 | 13 | 5/9 |
| 03 | 3/6 | 14 | 6/9 |
| 04 | 8/10 | 15 | 3/5 |
| 05 | 5/10 | 16 | 3/5 |
| 06 | 6/9 | 17 | 9/10 |
| 07 | **seg_fail** | 18 | 5/7 |
| 08 | **1/7** | 19 | 3/5 |
| 09 | **1/7** | 20 | **2/5** |
| 10 | 5/10 | | |

保留集低分張：floor_25（2/6）、floor_21/29（3/6）。

### 初步觀察（待逐張疊圖確認）

1. **分割層是最大失因**：floor_07 seg_fail、floor_08/09 只配 1/7、floor_20
   2/5——配上的 IoU 都 0.95+，代表其餘房間沒被切出（黏成大塊或漏切），
   不是切歪。這一項至少吃掉開發集 ~25 間。
2. **命名層系統性弱點**（兩集一致）：
   - **Balcony R=0.0（兩集全滅）**，開發集 6 個被叫成 Stair——陽台磚紋
     疑似觸發樓梯特徵。
   - **Bath R 低**（dev 0.22 / holdout 0.46），常被叫 Bedroom/Storage。
   - **Kitchen R=0.33**（兩集同數字）。
3. **比例尺**：color 圖開口樣本常為 0（floor_30 量出 0.46 cm/px、開口樣本
   0、退回 wall_mid），門寬鐵律在 color 線稿上偵測不到門弧，比例尺品質
   待查。

## 失因加權歸類（開發集，GT 142 間＝命中 84＋漏切 58；命中中叫錯 46）

漏切 58 間的類別分佈（GT 總數 − 混淆矩陣列和）：

| 類別 | 漏切/GT | 備註 |
| :--- | :--- | :--- |
| Balcony | 16/28 | 切出來的 12 個又全叫錯 → 端到端 0/28 |
| Bath | 10/28 | 配對到的 18 個只有 4 個叫對 |
| Bedroom | 10/39 | |
| LivingRoom | 8/18 | |
| Kitchen | 7/16 | 配對 9 個只對 3 |
| Hallway | 5/9、Entry 1/2、Storage 1/2 | 小類 |

**加權排序（影響間數，dev）**：

1. **分割黏連/漏切 ~35 間**——floor_07（seg_fail，10 間全失）、floor_08
   （整戶黏成一塊，6 間失）、floor_09（右半一塊＋左半漏切，6 間失）、
   floor_20（邊界跨房，3 間失）＋散在各張的單間漏切。疊圖：
   `temp/eval_rooms/chk/color_floor_{08,09,20}_pred.png`。
2. **Balcony 線 28 間**——漏切 16＋命名全錯 12（其中 6 個 → Stair）。
3. **Bath 命名 14 間**——誤向 Bedroom(6)/Storage(4)/Hallway(4)。
4. **Kitchen 12 間**——漏切 7＋叫錯 6（誤向 LivingRoom/Bath/Bedroom）。
5. **Hallway 過報**——P=0.23，各類都往 Hallway 漏（過切殘塊常被叫走道）。
