# RoomPilot AI 協作指引

根目錄 `AGENTS.md` 與 `CLAUDE.md` 必須保持完全相同。這兩份檔案是
不同 AI 工具的入口，不是另一套產品規格。

## 開始工作前

1. 完整閱讀 `docs/RoomPilot_現行版本總覽.md`。它是現行架構、流程、
   模組責任、資料邊界與接入狀態的唯一導航。
2. 安裝、啟動與常用測試依 `README.md`。
3. 修改特定模組前，閱讀總覽「正式契約」列出的對應
   `docs/contracts/` 文件。
4. 修改 Step 6／7 前，另讀 `docs/QUESTIONNAIRE_STEP6.md` 與
   `docs/SCHEME_WORKBENCH_CHANGELOG_2026-07-23.md`。
5. 若文件、程式與測試衝突，優先順序為：自動化測試、可執行程式、
   正式契約、現行版本總覽。先查明差異，不可用過時文件覆蓋現行行為。

## 不可破壞的邊界

- `backend/server/main.py` 是唯一正式 FastAPI；不得加入第二套後端或
  重複前端。
- 跨模組長度、平面座標、位移與淨空使用公分；新欄位使用 `_cm`，
  面積使用 `_m2`，角度使用度數。
- Agent 負責需求理解、選件、排序與修復策略；合法家具座標、旋轉、
  碰撞與淨空必須交由 `backend/engine/`。
- 前端不得自行建立第二套家具擺放、碰撞、淨空或合法座標演算法。
- 任何家具替換或移除後都要重新呼叫家具引擎驗證。
- 無法可靠對應的家具或模型必須隔離，不得出現在 UI、Agent 或 3D
  場景。
- 契約標示為提案、`pending` 或尚未接入的能力，不得宣稱已完成。
- OpenRouter 是可選能力；缺少或失敗時，本地核心流程仍須可用。
- 不直接整包合併舊分支。只移植各模組唯一落點所需內容，並保留使用者
  尚未提交的修改。

## 修改與驗證

- 變更範圍要小，不能順手改寫不相關檔案。
- 未獲使用者明確要求，不得 commit、push、刪除分支或丟棄工作樹修改。
- 不得提交 `.env`、憑證、個人工具設定、未驗證大型資產或暫存輸出。
- 修改後至少執行與變更直接相關的測試及 `git diff --check`；聲稱完整
  通過前，必須執行總覽與 README 指定的完整驗證。
- Step 6／7 的瀏覽器驗收使用 `floor04.png`，不得恢復 630 cm 舊圖的
  測試按鈕或以舊圖結果取代現行 E2E。
- 修改任一 AI 協作入口時同步另一份，並執行
  `uv run pytest tests/test_ai_instruction_docs.py
  tests/test_project_documentation_consistency.py -q`。

## 文件責任

不要把會變動的步驟數量、接線狀態、模型數量或分支進度只寫在本檔。
先更新 `docs/RoomPilot_現行版本總覽.md` 或相應正式契約，再同步本入口
中真正需要 AI 立即遵守的規則。
