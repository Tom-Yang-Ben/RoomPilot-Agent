# Engine 規則討論與測試紀錄 — 2026-07-29

> 個人工作筆記，比照 `engine開發紀錄_2026-07-24.md` 慣例**不 commit**。

## ★ 給接手 AI：閱讀順序

1. **本檔全文**——session 交接總帳與已拍板方向。
2. `engine淨空定義_2026-07-29.md`——淨空 v1.2 規格（待使用者勾選審查）。
3. `.claude/context/decisions/architect-2026-07-29-2010-engine-rules-advisory.md`——若存在：18 條建議＋落地順序（參考用）。
4. `.claude/context/decisions/general-purpose-2026-07-29-1940-engine-step-mapping.md`——engine 在八步的位置＋測試實跑。
5. 標準入職：`AGENTS.md` → `README.md` → `docs/TEAM_AI_OWNERSHIP.md` → `docs/owners/ANCAI.md` → `backend/engine/`。

**開發鐵律**：只動 `backend/engine/`＋tests；跨資料夾先問；先白話討論、分批拍板後才寫碼；RAG/LLM 不碰幾何；公分制；結構化中文失敗原因。


## ★ 工程完成狀態總表（AI 必維護）

> 最後更新：2026-07-30（第 6 步所有家具皆無法旋轉／待查）。任何項目完成、卡住、延後，AI 都要立刻改這張表，並主動告知 Ancai。
> 狀態只用：`完成` / `進行中` / `待你拍板` / `未做` / `下一輪` / `不歸我（只追蹤）`。
> **不可**把「規格寫了」說成「引擎做完」；不可把「引擎測過」說成「第 6 步已啟用」。

### 總進度一覽

| 階段 | 狀態 | 說明 |
|---|---|---|
| 1. 寫規格、講清楚差多少 | **完成** | 2026-07-29 八項均已勾選拍板 |
| 2. 只寫引擎＋測試 | **完成** | v1.2+v1.3；核心 50 passed；不接正式 UI |
| 3. 正式第 6 步接線啟用 | **部分完成（2026-07-30）** | server adapter 已套類型預設淨空；房型策略／配套關係未接 |

### A. 規格與拍板

| 項目 | 狀態 | 備註 |
|---|---|---|
| 淨空欄位定義（理想／最低、硬／軟） | 完成（規格） | 見 `engine淨空定義_2026-07-29.md` |
| 64 類對照表草案 | 完成（規格） | 含鏡櫃／屏風／兒童代碼 |
| 衣櫃最低 50、理想 90 | 完成（已拍板） | 2026-07-29 |
| 書櫃／書桌改提醒不硬擋 | 完成（已拍板） | 2026-07-29 |
| 沙發／沙發床不設前方淨空 | 完成（已拍板） | 2026-07-29 |
| 每種家具查自己的詳細規則 | 完成（已拍板） | 2026-07-29 |
| 配套家具可進舒適空間、不可擋門抽屜 | 完成（已拍板） | 2026-07-29 |
| 無法分推拉門時先當普通門 | 完成（已拍板） | 2026-07-29 |
| 鏡櫃／屏風／兒童系統代碼 | **完成** | 已從 `roompilot_type_category_mapping.csv` 確認 |
| 產品流程：問卷→JSON→RAG→Agent→換小 | 完成（方向拍板） | Agent 換小邏輯已存在 |
| 三階段節奏：規格→引擎→啟用 | 完成（方向拍板） | |

### B. 引擎實作（本輪目標；拍板後才做）

| 項目 | 狀態 | 備註 |
|---|---|---|
| 開門／抽屜：理想值＋最低值 | **完成** | 舊 depth 相容；新 ideal_cm／floor_cm |
| 失敗原因結構化（哪件、哪條、差幾 cm） | **完成** | PlacementIssue／PlacementValidation；舊 reason 保留 |
| 配套家具可進舒適使用空間 | **完成（engine API）** | companion_pairs；不豁免門／抽屜 |
| 舒適空間（書櫃／書桌）記錄／警告、不硬擋 | **完成（engine API）** | access 只進 warnings |
| 沿牆／候選搜尋改善 | **完成** | 中心→四面牆→15cm 室內網格；仍是 first-fit |
| 小房測試：240×240 單人床＋衣櫃（衣櫃最低 50） | **完成** | 找到合法位置並由淨空總入口複驗 |
| 旋轉後淨空方向測試 | **完成** | 90°、180° 均有測試 |
| 並排櫃淨空邊界是否假違規測試 | **完成** | 只有邊界相接不算衝突 |
| 家具詳細類型預設表 | **完成（opt-in）** | `clearance_defaults.py`；不自動改第 6 步 |
| JSON `clearance_zones` | **完成（v1.3）** | 支援多面 + `clearance_mode` / `clearance_enforce_floor` |
| Engine README | **完成** | 已記錄 v1.2／v1.3 與未啟用邊界 |

### 2026-07-29 v1.2 完成摘要

- 完成：雙層淨空、必要／舒適分流、配套豁免、結構化失敗、原始詳細類型預設、中心／沿牆／15cm 網格搜尋、JSON 單面契約。
- 驗證：核心 42 passed；相關 28 passed；完整 518 passed / 1 個 Bella 既有失敗 / 5 skipped；lints 0；`git diff --check` 通過。
  - 5 skipped：4 個是已退役的 10,550 筆舊 catalog 測試；1 個因未設定外部離線 GLB 備援包。
  - 3 warnings：FastAPI `on_event` 棄用警告 2 則；Starlette TestClient／httpx 棄用警告 1 則；都非本批引擎錯誤。
- 尚未啟用：正式第 6 步、Kai catalog 欄位、Bella adapter。
- v1.3 已補：餐桌有椅才留面、床至少一側。仍未做：走道連通、美感評分、第 6 步啟用。

### C. 下一輪（AI 開工前必須主動提醒）

| 項目 | 狀態 | 備註 |
|---|---|---|
| 餐桌有椅才留面 | **完成（v1.3 engine）** | 未接第 6 步；餐椅可進配套 |
| 床兩側至少留一側 | **完成（v1.3 engine）** | 未接第 6 步 |
| 房型策略表（預設清單／順序／降級） | **進行中／待勾選** | 見下方「房型策略表 v0」 |
| 走道連通檢查 | 下一輪 | |
| 擺放評分／美感最佳化 | 下一輪 | |
| 正式第 6 步啟用 | 下一輪／跨 owner | |
| 第 6 步所有家具皆無法旋轉／另有錯件床→衣櫃 | **待查** | 見下方 2026-07-30 QA |

### D. 不歸我，但要追蹤

| 項目 | 狀態 | 誰 |
|---|---|---|
| catalog 寫入 clearance 欄位 | 未做 | Kai |
| `style_db.CLEARANCE_BY_TYPE` 收斂 | 未做 | 啟用時協調 |
| 窗前禁放／沿牆候選從 scene_service 收回 | 未做 | 跨 Bella，先問 |
| layout_rules 500 bug | 不歸我（只追蹤） | Bella |
| scene_v2 cache key | 不歸我（只追蹤） | Bella |

### 維護規則（給所有接手 AI）

1. 每完成一項：把狀態改成 `完成`，並在當日摘要加一行「做了什麼／怎麼驗證」。
2. 每延後一項：改成 `下一輪`，並確認「下一輪主動提醒」區塊仍列得到。
3. Ancai 問「做到哪了」時：先貼這張總表，再講細節。
4. 不得把測試中、半成品、只寫了註解的項目標成完成。

---

---

## ★ 2026-07-29 晚間已拍板方向（Cursor 討論後寫入）

### 推進節奏（三階段，不可跳）

1. **先寫規格、講清楚差多少** ← **已完成**（2026-07-29 八項均已勾選拍板）。
2. **只寫引擎＋測試** ← **已完成**（2026-07-29；第 6 步正式 UI 尚未接）。
3. 引擎就緒後 → 再請 Bella／Kai **接線啟用**。

### 產品資料流（你口述）

```text
前面步驟：確定尺寸＋空間結構＋問卷
        ↓
產出 JSON（需求／約束；風格等非幾何欄位原封往後流）
        ↓
RAG 選家具（可超供）
        ↓
Agent 在規則下呼叫引擎擺放
        ↓
放不下 → 換小款（「放得下」優先）
        ↓
剩餘／證據可當第 8 步生圖素材
```

補充：`backend/agent/place.py` **已有** `pick_smaller_model`／`resolve_placements`。規格確認的是策略，不是新發明換小邏輯。

### 契約原則（待正式啟用時再跨 owner 對齊）

1. 一份 JSON 兩個消費者：engine 只讀幾何；風格／mood／rag_text 流到生圖。
2. 超供可接受：依優先序擺；擺不下誠實回報；剩餘標記後當生圖素材。
3. 人數兩路：幾人座＝RAG 選件；走道寬等幾何參數＝換成數字給 engine。

### 淨空審查入口

詳見 `engine淨空定義_2026-07-29.md`。AI 已排好的關鍵修正：

| 項目 | 決定 |
|---|---|
| 衣櫃底線 | **50**（不是草案 60）；理想 90 |
| 書櫃／書桌 | 前方空間不足先提醒，不直接禁止擺放 |
| 沙發／沙發床 | **不設**前方淨空 |
| 家具種類判斷 | 每種家具查自己的詳細規則，不先合併成大類 |
| 配套家具 | 椅子可放在桌前的舒適使用空間，但不可擋門或抽屜 |
| 配套豁免可寫性 | 可做，成本小～中 |

### 與別人職責衝突分類（提醒）

#### A. 適合收回／新建在 engine（動之前要問跨資料夾）

| 項目 | 現在在哪 | 提醒 |
|---|---|---|
| 出界／撞牆／重疊／開合淨空 | 已在 engine | 本來就是你的 |
| 沿牆／網格找可放位置 | 大半 `scene_service` | 「能不能放」的搜尋屬幾何 |
| 窗前禁放帶 | `scene_service.window_clearance_zones` | 合法性不該只活在 server |
| 地毯可壓、壁架略過本體碰撞 | `scene_service` 白名單 | 應變成引擎層級／規則 |
| 結構化失敗（哪件、哪條、差幾 cm） | 現在純字串 | Agent 換小／重選需要 |

#### B. 留在別人那邊（不要搶）

| 項目 | 誰 |
|---|---|
| RAG 選件、風格／人數→幾人座 | Yen／Kai |
| 問卷、八步 UI、專案保存 | Bella |
| catalog／GLB／DB 淨空欄位落地 | Kai（你定契約） |
| 「哪面牆比較美」類美感偏好 | 暫留 Bella／之後再議 |
| layout_rules 500、cache key | Bella（只回報） |

#### C. 邊界模糊

| 項目 | 建議 |
|---|---|
| `_placement_candidates` | 合法性搜尋進引擎；好看順序可暫留 server |
| 換小款 | **Agent 做**；引擎只回放不下＋原因 |
| 門／窗進 `Room` | 應進引擎模型；資料仍來自 `layout_json` |

---

## 淨空表待驗證點（批判代理曾中斷；規格已採納部分結論）

1. ~~衣櫃 60 小房無解？~~ → **規格改底線 50**，理想仍 90。寫引擎時仍應用測試鎖定 240×240 床＋衣櫃可行。
2. 旋轉語意：zone 跟 rotation 轉——靠牆衣櫃 front 是否朝室內？寫測試證明。
3. zone-vs-zone：並排櫃 front 是否假違規？驗證斜角／轉角。
4. ~~家具大類合併造成規則衝突~~ → **改成每種家具查自己的詳細規則**；沙發床當沙發處理、不設前方淨空。
5. 配套家具：椅子可以放進桌子的舒適使用空間，但不能擋門或抽屜；要先讓引擎認得配套關係，再啟用舒適空間檢查。
6. 值變更衝擊：wardrobe 底線同 50；sideboard／抽屜變嚴；bookcase／desk 變軟——啟用前要回歸測試清單。

---

## 已確立的事實（有證據）

### 測試實跑（WSL）
- 歷史基準：**502 passed / 3 failed / 5 skipped**。
- v1.2 完成後全套：**518 passed / 1 failed / 5 skipped**；唯一失敗是 Bella 既有 `scene_v2.js` cache key，與本批無關。
- 本批核心：`test_clearance.py`＋`test_placement.py` **42 passed**；agent／scene 相關 **28 passed**；IDE lints 0。
- 3 個真實失敗屬 Bella（layout_rules 字串 500、scene_v2 cache key）。
- 常駐 server 8002 勿動；測試曾留 `engine-e2e-test` 專案。

### Engine 八步定位
- Production 進入點：`/api/scene/generate|layout|validate|decorate`（第 6 步）＋ DXF analyze（第 2 步，僅 `dxf_room`）。
- 唯一 production import：`scene_service.py`；`adjustment`／`schema` production 零 import。
- Agent 不直接 import engine，靠注入 `engine_place_fn`。
- 引擎只算 2D footprint；3D 是同一 scene_json 呈現。

### 核心觀察（仍有效）
- `placement.py`：已從 13 點×95cm 升級為中心→四面牆→15cm 室內網格；仍是 first-fit，美感評分尚未做。
- `Room` 仍無門／窗／多邊形；`ClearanceZone` 本輪仍單面，但已支援必要／舒適、理想／最低。
- 失敗已新增結構化 `PlacementIssue`／`PlacementValidation`；舊字串介面保留相容。
- Catalog 無淨空欄位；中英詞彙要對齊。

---

## 建議落地順序（規格 OK 後）

1. ~~使用者勾選淨空檢查清單~~ → **2026-07-29 已完成**。
2. ~~引擎：理想／最低＋結構化失敗~~ → **已完成**。
3. ~~引擎：配套家具舒適空間豁免~~ → **已完成（engine API）**。
4. ~~引擎：改善找位置（沿牆／候選）~~ → **已完成；未接 UI**。
5. （跨 owner）Kai 填 clearance 欄位；Bella 接線；拿掉／收斂 `style_db.CLEARANCE_BY_TYPE` 與 server 重複合法性。

**已做完仍未接 UI**：多面淨空、床至少一側、餐桌有椅才留面。
**仍不做／延後**：正式第 6 步啟用、走道連通、美感評分（除非另拍板）。

### 下一輪開工前必須主動提醒 Ancai

**已於 v1.3 完成（engine＋測試；仍未接第 6 步）**：
1. 餐桌有椅才留面（呼叫端宣告面；預設可四面）；餐椅可進配套 access。
2. 床左右長側至少一側（理想 75／最低 60）；床尾不算；sofa-bed 不套用。

**仍未做，接手時必須主動提醒**：
1. 走道連通檢查
2. 美學／擺放評分（first-fit → 評分選優）
3. 正式第 6 步接線（跨 Bella／Kai）
4. 房型策略表（預設清單＋順序＋降級）— **明天優先討論拍板**
5. 類型預設覆蓋率仍低（~22／~90 類型有 clearance 預設；且 opt-in）
6. 已知對碼缺口：`chest-of-drawers`（資料）vs `chests-of-drawer`（engine 預設鍵）

---

## 需要你提供／勾選

1. 審查 `engine淨空定義_2026-07-29.md` 第四節八個白話確認項目。
2. `家具大分類_小分類統計.csv` 已在 repo 根目錄——房型白名單對齊時可用。
3. JSON 觸發時點（問卷後／進第 5 步前）若有更精準草稿，之後對契約時補。

## 紅線提醒

- 只擁有 `backend/engine/`＋對應 tests；動其他 owner 先問。
- 白話討論、一批一次拍板才動工。
- RAG／LLM／瀏覽器永不決定幾何。
- Bella 的 layout_rules／cache key 只回報不動手。
- 這兩份 md **不 commit**。

## ★ 工程完成狀態總表（續・v1.3 2026-07-29）

| 項目 | 狀態 | 驗證 |
|---|---|---|
| 床左右長側至少一側（ideal 75 / floor 60） | 完成 | `tests/test_clearance.py` 床相關 |
| 床尾不計入側 | 完成 | `test_bed_foot_block_does_not_count_as_side_clearance_failure` |
| sofa-bed 不套床側 | 完成 | `test_sofa_bed_default_has_no_bed_side_rule` |
| 餐桌有椅才留面（呼叫端宣告／預設四面） | 完成 | dining 相關測試 |
| 配套椅可佔餐桌 access | 完成 | `test_dining_chair_companion_may_occupy_table_access_zone` |
| ClearanceSpec + schema 多面 | 完成 | `test_catalog_json_accepts_multiple_clearance_zones_with_mode` |
| 正式第 6 步接線 | 未做 | — |
| 走道連通／美學評分 | 未做 | 延後提醒 |

### 驗證紀錄（v1.3 實測）

- 核心：`tests/test_clearance.py` + `tests/test_placement.py` → **50 passed**
- 相關：agent_place / soft_decor / layout_regions → **28 passed**
- 全庫（Cursor bundled Node v22）：**523 passed / 4 failed / 5 skipped**
  - 失敗 1：Bella `scene_v2.js` cache key（既有，非本輪）
  - 失敗 2–4：floorplan vision e2e 於全庫並跑曾報 `scene_service` AttributeError；單獨重跑 3 案 **passed**（非 engine 迴歸）
- `git diff --check`：乾淨（engine／tests）
- 本輪未改 `backend/server/`、未接第 6 步

---

## ★★★ 明天接續開發交接（寫於 2026-07-30 01:30）

> 給 Ancai／下一輪 AI：先讀本節，再動工。本檔與 `engine淨空定義_2026-07-29.md` **不 commit**。

### 一句话现状

家具淨空引擎 **v1.2＋v1.3 已寫完並測過**（含床至少一側、餐桌有椅才留面），但是 **opt-in、尚未接入正式第 6 步**。資料庫也 **不是每種類型都有預設淨空**。明天先把「房型要放什麼」定清楚，再談接線。

### 今日已完成（可接續依賴）

| 區塊 | 內容 | 位置 |
|---|---|---|
| v1.2 | 雙層淨空 ideal/floor、operation/access、companion_pairs、結構化失敗、15cm 搜尋 | `backend/engine/` |
| v1.3 | `ClearanceSpec`（all/any + enforce_floor）；床左右至少一側；餐桌多面；schema 多面 JSON | 同上 |
| 預設表 | `clearance_defaults.py`（約 22 類型） | **opt-in**，第 6 步未自動套 |
| 測試 | 核心 **50 passed**；相關 28；全庫約 523 passed（Bella 既有失敗另計） | `tests/test_clearance.py` / `test_placement.py` |

未提交的引擎改動（工作區）：

- 已改：`models.py` `clearance.py` `placement.py` `schema.py` `README.md` + 兩個 tests
- 新增：`backend/engine/clearance_defaults.py`
- **不要**把個人 md／csv／VibeCoding 模板誤 commit

### 今日已釐清的產品認知（討論結論）

1. **預設放什麼**（客廳沙發＋電視櫃＋茶几；臥室床＋衣櫃＋床頭櫃）＝房型策略表，可先定。
2. **正確擺進去**＝最終由 **Engine** 找座標＋驗證；Agent 只做選件／順序／關係意圖／換小，**不算幾何**。
3. **電視**：正式家具是 **電視櫃 `tv-bench`（約 49）**＋`tv-media-furniture`（約 10）；**沒有**獨立電視機類型進自動擺設。
4. **電視櫃現況位子**：Bella `scene_service` 硬編碼靠上牆候選 → 不成再丟 engine 網格；不是 Agent 定座標。
5. **覆蓋率**：mapping ~90 類型中約 **22** 有引擎預設淨空；其餘多半只做碰撞。沙發／茶几等規格常是「不設前方淨空」或軟裝。
6. **走道連通**＝人走不走得過（合格線上移）；**美學評分**＝合法解裡選好看的（較後做）。

### 明天建議順序（先討論再寫碼）

| 優先 | 項目 | 產出 | 注意 |
|---|---|---|---|
| **P0** | 房型策略表 v0 拍板 | 客廳／臥室／餐廳：必備、可選、順序、關係意圖、放不下怎麼砍 | 先文件勾選，不改 Bella |
| **P1** |（可選小修）對碼 | `chest-of-drawers` 對上 engine 預設鍵 | 只動 engine defaults |
| **P2** | 討論第 6 步接線範圍 | 誰呼叫 `catalog_with_default_clearance`；要不要動 server | **跨 owner 先問 Bella** |
| 延後 | 走道連通 | — | 提醒即可 |
| 延後 | 美學評分 | — | 最後 |
| 不歸我 | scene_v2 cache key、`layout_rules` 字串 `.get` 500 | — | 只回報 Bella |

### 明天開場白（給 AI 直接用）

```text
接續 Ancai engine 工作。先讀 engine規則討論紀錄_2026-07-29.md 的「明天接續開發交接」。
v1.2/v1.3 引擎已完成且核心 50 tests 綠，尚未接第 6 步。
今天目標：先產出並讓 Ancai 勾選「房型策略表 v0」（客廳／臥室／餐廳），先不寫正式接線、不做走道／美學。
鐵律：只動 backend/engine + tests；跨資料夾先問；個人 md 不 commit。
```

### 驗證指令（明天回歸用）

```bash
.venv/bin/python -m pytest -q tests/test_clearance.py tests/test_placement.py
.venv/bin/python -m pytest -q tests/test_agent_place.py tests/test_scene_soft_decor.py tests/test_scene_layout_regions.py
```

### 紅線（不變）

- RAG／LLM 不決定幾何；合法位置只由 engine 判定。
- 家電不進 2D/3D 自動配置。
- quarantine 不當正式家具。
- 未問過不得改 `backend/server/` / `backend/agent/` / catalog SQL。

---

## ★★★ 房型策略表 v0（2026-07-30・待 Ancai 勾選）

> 本節是 **P0 產出**：先勾選拍板，再談實作與第 6 步接線。  
> **不改** `backend/server/`／`backend/agent/`。個人 md **不 commit**。  
> 類型鍵對齊 catalog `normalized_type`（kebab-case）。

### 職責邊界（再確認）

| 誰 | 做什麼 | 不做什麼 |
|---|---|---|
| 本表（策略） | 每房「要放什麼／順序／關係意圖／放不下怎麼砍」 | 不算座標、不算碰撞 |
| Agent（Yen） | 依本表選件、排序、宣告 companion、換小款 | 不算幾何合法性 |
| Engine（Ancai） | 找座標＋驗證淨空／碰撞；回結構化失敗 | 不決定房型清單 |
| RAG／LLM | 風格／證據／超供候選 | 不定幾何、不定合法性 |

### 對照現況（僅參考，不是拍板）

現行 Bella `SPACE_DEFAULTS`：

- 客廳：`sofa, coffee-table, tv-bench, armchair, bookcase`
- 臥室：`bed, bedside-table, bookcase, runner-small-rug`（**無衣櫃**）
- 餐廳：`dining-table, dining-chair, sideboard`

昨晚口述共識：客廳沙發＋電視櫃＋茶几；臥室床＋衣櫃＋床頭櫃。電視＝`tv-bench`／`tv-media-furniture`，**無獨立電視機**進自動擺設。

### 通用降級規則（三房共用・請勾）

- [ ] **G1** 放不下 → 先換同類型較小款（Agent `pick_smaller_model`），再砍可選件
- [ ] **G2** 砍件順序＝由清單**尾端往前**（先砍可選，最後才碰必備）
- [ ] **G3** 必備全砍仍放不下 → 誠實回報失敗（結構化 issue），剩餘標成第 8 步生圖素材，不硬塞
- [ ] **G4** 地毯／燈／抱枕等軟裝 **不進**本表自動配置（維持現有 soft-decor／生圖路徑）
- [ ] **G5** 家電（冰箱／洗衣機等）**永不**進 2D/3D 自動配置

---

### 客廳 `living_room`

| 順序 | 角色 | 類型鍵 | 建議 | 關係意圖 | 引擎淨空現況 |
|---|---|---|---|---|---|
| 1 | 錨點必備 | `sofa`（可含 fabric/leather/modular 對應到 sofa 家族） | ☐ 必備 | 靠牆優先（合法性搜尋）；不設前方淨空 | 無前方預設（刻意） |
| 2 | 必備 | `tv-bench`（備援 `tv-media-furniture`） | ☐ 必備 | 與沙發對望／對牆；無獨立 TV | 有 operation 60/45 |
| 3 | 必備 | `coffee-table` | ☐ 必備 | 沙發前方配套（access companion） | 無預設淨空 |
| 4 | 可選 | `armchair` | ☐ 可選 | 次座位；可砍 | 無預設 |
| 5 | 可選 | `bookcase` / `shelving-unit` | ☐ 可選 | 靠牆收納；可砍 | access 75/40 |
| — | 不放 | 獨立電視機、家電 | ☐ 同意不放 | — | — |

**客廳放不下砍序（建議）**：armchair → bookcase → coffee-table →（換小 sofa/tv-bench）→ 仍失敗才報錯  
請勾：☐ 同意此砍序　☐ 改成：________

**備註選項**：
- [ ] 茶几改「可選」（小客廳常放不下）
- [ ] 維持茶几必備
- [ ] `tv-bench` 與 `tv-media-furniture` 視為同一槽（二擇一）

---

### 臥室 `bedroom`

| 順序 | 角色 | 類型鍵 | 建議 | 關係意圖 | 引擎淨空現況 |
|---|---|---|---|---|---|
| 1 | 錨點必備 | `bed` / `bed-frame` | ☐ 必備 | 左右長側至少一側可上下床；床尾不算側 | ClearanceSpec any 75/60 |
| 2 | 必備 | `wardrobe` / `pax-wardrobe` | ☐ 必備 | 靠牆；門扇開啟空間 | operation 90/50 |
| 3 | 必備 | `bedside-table`（一或兩件） | ☐ 必備 | 床側配套；可進床側 access | 無預設淨空 |
| 4 | 可選 | `chests-of-drawer`（資料鍵常為 `chest-of-drawers`←P1 對碼） | ☐ 可選 | 靠牆收納；可砍 | operation 90/60（鍵需對） |
| 5 | 可選 | `bookcase` | ☐ 可選 | 可砍；**不要**用地毯當預設必備 | access 75/40 |
| — | 不放 | `runner-small-rug` 當自動必備、家電 | ☐ 同意不放（改軟裝） | — | — |

**與現行 Bella 差異**：現行預設有 bookcase＋小地毯、**無衣櫃**；本表改為有衣櫃、地毯移出自動配置。

**臥室放不下砍序（建議）**：bookcase → chests-of-drawer → 第二床頭櫃 →（換小 bed/wardrobe）→ 仍失敗才報錯；**至少保留床**  
請勾：☐ 同意此砍序　☐ 床頭櫃改可選　☐ 衣櫃改可選（不建議）

**床頭櫃數量**：
- [ ] 預設 1
- [ ] 預設 2（放不下砍成 1）

---

### 餐廳 `dining_room`

| 順序 | 角色 | 類型鍵 | 建議 | 關係意圖 | 引擎淨空現況 |
|---|---|---|---|---|---|
| 1 | 錨點必備 | `dining-table` | ☐ 必備 | 有椅才留面；呼叫端宣告有椅側 | ClearanceSpec all 75/60 |
| 2 | 必備（組） | `dining-chair` ×N | ☐ 必備 | 餐桌配套；可進 access zone；N 由人數／RAG | 無椅自身預設 |
| 3 | 可選 | `sideboard` | ☐ 可選 | 靠牆；可砍 | operation 90/60 |
| 4 | 可選 | `display-cabinet` | ☐ 可選 | 可砍 | operation 60/45 |
| — | 備援桌 | `table` / `bar-table` | ☐ 僅當無 dining-table 時 | bar 預設前後兩面 | 已有 spec |

**椅數 N（建議）**：
- [ ] 跟問卷人數（常見 2/4/6），上限 6
- [ ] 固定先嘗試 4，放不下減到 2
- [ ] 其他：________

**餐廳放不下砍序（建議）**：display-cabinet → sideboard → 減椅（6→4→2）→ 換小桌 → 仍失敗才報錯  
請勾：☐ 同意此砍序　☐ 改成：________

---

### 刻意不做（本輪）

- [ ] 確認：走道連通、美學評分、正式第 6 步接線 — **本輪不寫**
- [ ] 確認：書房／玄關／廚房另表，不塞進 v0
- [ ] 確認：勾選完成前 **不改 code**（P1 `chest-of-drawers` 對碼可另開小修）

### 勾選後下一步（勿提前做）

1. 把拍板結果寫回狀態總表 → 「房型策略表 v0 **已拍板**」
2. 可選 P1：defaults 鍵 `chest-of-drawers`
3. P2：與 Bella 討論誰呼叫 `catalog_with_default_clearance`（跨 owner 先問）

---

## 2026-07-30 第 6 步最小淨空接線紀錄

- 已獲 Ancai 同意：只為測試淨空定義接線，不改房型清單。
- `backend/server/scene_service.py` 在場景家具轉 Engine catalog 時套用 `catalog_with_default_clearance()`。
- 舊 `style_db.CLEARANCE_BY_TYPE` 是 adapter 類型預設，不是單品明示值；server 先清除再套 Engine v1.3 SSOT。
- 新增第 6 步整合測試：床至少一側 60cm、餐邊櫃最低開合 60cm。
- 尚未接：房型策略、餐桌椅 companion 關係與有椅面宣告、理想值警告 UI。
- 驗證：Engine／Agent／Scene 聚焦測試 80 passed。

---

## 2026-07-30 QA：第 6 步所有家具皆無法旋轉（先記錄、未修）

> 來源：Ancai 在第 6 步手動測；先記錄，不立刻改碼。

### 現象

- 清單第 **2** 件，左側顯示 **床**，右側狀態 **合法**。
- 顯示名稱：`Vida Designs Corona Wardrobe, 3 Door, Solid Pine Wood...`（名稱是 **Wardrobe／衣櫃**，不是床）。
- 尺寸顯示：**150 × 200 cm**。
- 初次回報：放在側邊時不能旋轉。
- **12:16 補充：第 6 步所有家具皆無法旋轉，並非單件或單一方向。**

### 初步懷疑（未驗證）

1. **全域 UI／事件流程問題（優先）**：所有家具都不能轉，較像旋轉按鈕、事件綁定、選取狀態或 API 提交流程失效（偏 Bella），不是某一類淨空規則。
2. **後端旋轉驗證共同被拒**：若 UI 有送出 90°，需查 validate／resolve 回傳是否全面拒絕，以及前端是否吞掉 `reason`。
3. **錯件是另一筆問題**：該件 `normalized_type` 疑似為 `bed`，名稱卻是 wardrobe；要獨立追查 catalog 對碼，不再視為「全部不能旋轉」主因。

### 建議下次查法

1. 先確認旋轉控制是否觸發事件，以及 request 是否送出新的 `rotation_y_deg`。
2. 對任一空曠處小家具手動轉 90°，記錄 validate／resolve 的 HTTP status、`ok`、`reason`。
3. 若 request 根本沒送出，查前端事件／選取狀態；若有送出但全面失敗，再查後端共同驗證。
4. 另行確認 catalog 是否把 wardrobe 誤選進 bed slot。

### 狀態

- **只記錄，未修。**
- 不歸房型策略表；與淨空接線／錯件選型可能相關。
