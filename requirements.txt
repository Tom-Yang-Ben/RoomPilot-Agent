# FastAPI web runtime for the RoomPilot demo site.
fastapi>=0.111,<1.0

# ASGI server. Run with:
# python -m uvicorn web_fastapi.main:app --reload
uvicorn[standard]>=0.30,<1.0

# Used by web_fastapi/main.py to read embedded images from GLB files.
pillow>=10.0,<12.0
