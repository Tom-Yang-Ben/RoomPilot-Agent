numpy>=2.0
opencv-python>=4.10,<5        # 5.0 把 HoughLinesP 回傳 shape (N,1,4) 改成 (N,4)，門偵測會掛
opencv-python-headless<5      # PyTorch 生態會拉進 headless 版，後裝者蓋掉 cv2，同樣要擋 5.0
ezdxf>=1.3
svgpathtools>=1.6             # symbol_match 解析 SVG path（產品推論期依賴）
torch>=2.0                    # DINOv2 房型裁切分類（CPU 版即可，只推論不訓練）。
                              # 缺它房型會退回面積規則、品質明顯下降。骨幹權重 88MB
                              # 由 torch.hub 首次下載後快取於 ~/.cache/torch/hub/，
                              # 實測封鎖網路仍可載入——非執行期連網需求
rapidocr-onnxruntime>=1.4     # 房型 OCR 文字證據層（純 pip/CPU，免系統套件——tesseract 需 apt 故不採）
pytest>=8.0                   # training/tests/ 測試框架

# ── 以下僅 training/ 的研發工具需要，產品推論不碰 ──
lmdb>=1.4                     # CubiCasa svg_loader（GT 標注解析、訓練資料打包）
scikit-image>=0.24            # CubiCasa svg_utils
