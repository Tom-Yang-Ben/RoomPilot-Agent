# cody 辨識管線依賴（2026-08-03 終版對齊：版本＝實際開發環境 pip freeze）
# 產品推論鏈（backend/floorplan/）＋研發評測（training/）共用一份。

numpy==2.5.1
opencv-python==4.13.0.92        # 鎖 <5：5.0 把 HoughLinesP 回傳 shape (N,1,4) 改成 (N,4)，門偵測會掛
opencv-python-headless==4.13.0.92  # torch 生態會拉 headless 版蓋掉 cv2，必須同版同鎖
ezdxf==1.4.4
svgpathtools==1.7.1             # symbol_match 解析模板庫 SVG path（產品推論期依賴）
torch==2.13.0                   # DINOv2 房型雙頭＋分割頭（CPU 版即可，只推論不訓練）。
                                # 缺它房型退回面積規則、分割頭停用（皆出聲）。骨幹權重 88MB
                                # 由 torch.hub 首次下載後快取於 ~/.cache/torch/hub/，
                                # 實測封鎖網路仍可載入——非執行期連網需求
rapidocr-onnxruntime==1.4.4     # 房型 OCR 文字證據層（純 pip/CPU，免系統套件）
pytest==9.1.1                   # training/tests/ 測試框架

# ── 以下僅 training/ 研發工具需要，產品推論不碰 ──
scikit-image==0.26.0            # svg_poly 的 GT 多邊形點陣化（eval_rooms_cc 量測鏈）
# lmdb 已移除（2026-08-03）：CubiCasa svg_loader 退場後全 repo 零引用，殭屍依賴
