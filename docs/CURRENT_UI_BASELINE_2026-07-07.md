# Current UI Baseline 2026-07-07

## 決策

目前非 3D 的網站呈現以 `web_fastapi/static/` 內現有版本為主。

後續若組員 merge 新流程或新功能，請優先保留目前的：

- 首頁視覺與導覽架構
- `/styles` 風格類型頁排版
- `/library` 家具資料庫頁排版
- 中文化文案與家具篩選呈現

可以接入或替換的是：

- `/scene` 的 3D 生成流程
- DXF 解析流程
- LLM JSON 生成流程
- 家具配置、替換、移除與防碰撞邏輯
- 牆面與地板材質資料

## 回復點

目前已保留一份 UI 回復點：

`version_snapshots/current_ui_before_scene_merge_20260707/`

若後續 merge 後畫面跑掉，可以先比對這份 snapshot，再把需要的頁面呈現搬回來。

## 2026-07-07 本次整理

- `sf3d/metadata/ikea_furniture_style_database.json` 補入 13 種牆面與 18 種地板材質。
- 12 種風格已補牆面推薦、地板推薦與推薦搭配。
- 北歐風 `不要的元素` 已補「明顯跳色」與「高彩度撞色」。
- `/scene` 生成 API 已驗證：可生成家具、同風格替換、移除家具。
- 家具尺寸解析已補強：標題只有單一 `85 公分` 也會解析成尺寸資料，不會完全空白。

## 注意

如果瀏覽器畫面沒有更新，先重開 FastAPI 並強制重新整理瀏覽器，因為靜態檔與 JSON 可能被快取。
