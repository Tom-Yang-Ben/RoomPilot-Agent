# 路線圖 A：房型答案集評分工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use the Execute Plan phase of
> superpowers:sunnydata-design to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `eval_rooms_cc.py`——以 CubiCasa5k model.svg 的 Space 多邊形為 ground truth，對 floorplan2room 輸出算「房間分割 IoU＋房型混淆矩陣」。

**Architecture:** 單檔評分腳本，純 import 調用 floorplan2room 的內部呼叫鏈（probe_color → detect_bw/color → refine_scale → detect_symbols → build_rooms），零管線修改。GT 解析重用 CubiCasa5k 程式庫（rooms_selected 映射＋get_polygon 光柵化）。配對/指標為純函式，可單元測試。

**Tech Stack:** Python 3.12 .venv、numpy/cv2、floortrans.loaders（torch CPU 已裝）、pytest（新增 dev 依賴）

**對齊 spec:** docs/superpowers/specs/2026-07-15-room-eval-design.md

---

## 既定事實（探索結論，實作直接引用）

- 樣本清單行格式：`/high_quality_architectural/1191/`（strip `/` 取 id=`1191`）
- 預測房型 key 全集：`living/kitchen/bed/bath/balcony/room/entry/storage/garage/outdoor/None`
  （fp_c.ROOM_ZH ∪ f2r.ROOM_ZH_EX）；正規化 `balcony→outdoor`、`room/None→space`
- GT 12 類 id → 同 key：`f2r.CC_ROOM_LABEL`（3=kitchen,4=living,5=bed,6=bath,7=entry,9=storage,10=garage,1=outdoor），11→space，0/2/8 排除
- `build_rooms(det)` 回傳 `(labels, rooms, bridges, zones, edges)`；labels=房間連通塊 id 圖（原圖尺寸，彩圖管線可能 2 倍——classify_rooms_cc 內已有 resize 前例，eval 端同樣以 labels.shape 為準）
- 快取撞名：樣本圖全叫 `F1_scaled.png` → 先複製為 `eval_rooms/input/<id>.png`
- cfg 是 dataclass：`from dataclasses import replace`，`replace(cfg, input=path, output="", preview=None)`

## 檔案結構

- Create: `eval_rooms_cc.py`（評分主體，~260 行）
- Create: `tests/test_eval_rooms_cc.py`（純函式單元測試）
- Modify: `.gitignore`（排除 `eval_rooms/input/`——資料集複本不進版控）

---

### Task 1: 純函式測試先行（RED）

**Files:** Create `tests/test_eval_rooms_cc.py`

- [ ] **Step 1: 裝 pytest**

Run: `.venv/bin/pip install pytest`

- [ ] **Step 2: 寫失敗測試**

```python
"""eval_rooms_cc 純函式測試：配對、正規化、混淆矩陣。"""
import numpy as np
import pytest
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from eval_rooms_cc import match_rooms, norm_label, confusion


def _m(h, w, y0, y1, x0, x1):
    m = np.zeros((h, w), bool)
    m[y0:y1, x0:x1] = True
    return m


def test_match_perfect_overlap():
    gt = [_m(20, 20, 0, 10, 0, 10)]
    pred = [_m(20, 20, 0, 10, 0, 10)]
    got = match_rooms(gt, pred)
    assert got == [(0, 0, pytest.approx(1.0))]


def test_match_below_threshold_excluded():
    gt = [_m(20, 20, 0, 10, 0, 10)]          # 100 px
    pred = [_m(20, 20, 0, 10, 0, 4)]         # 交 40 聯 100 → IoU 0.4 < 0.5
    assert match_rooms(gt, pred) == []


def test_match_greedy_one_to_one():
    # 兩 GT 兩 pred：pred0 與 gt0 IoU 高、與 gt1 也重疊——貪婪應各配各的
    gt = [_m(30, 30, 0, 10, 0, 10), _m(30, 30, 10, 20, 0, 10)]
    pred = [_m(30, 30, 0, 11, 0, 10), _m(30, 30, 11, 20, 0, 10)]
    got = match_rooms(gt, pred)
    assert sorted((g, p) for g, p, _ in got) == [(0, 0), (1, 1)]


def test_norm_label():
    assert norm_label("balcony") == "outdoor"
    assert norm_label("room") == "space"
    assert norm_label(None) == "space"
    assert norm_label("bed") == "bed"


def test_confusion_counts():
    pairs = [("bed", "bed"), ("bed", "bath"), ("space", "space")]
    cm = confusion(pairs)
    assert cm["bed"]["bed"] == 1 and cm["bed"]["bath"] == 1
    assert cm["space"]["space"] == 1
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: FAIL（ModuleNotFoundError: eval_rooms_cc）

### Task 2: eval_rooms_cc.py 純函式（GREEN）

**Files:** Create `eval_rooms_cc.py`（先寫純函式段，讓 Task 1 綠燈）

- [ ] **Step 1: 建檔——docstring、import、常數、三個純函式**

```python
"""eval_rooms_cc.py — 路線圖 A：房型答案集評分。

CubiCasa5k model.svg 的 Space 多邊形當 ground truth，對 floorplan2room
的房間方塊算「分割 IoU＋房型混淆矩陣」——房型權重從拍腦袋變可量測。

樣本：val/test 的 high_quality_architectural 單層樓樣本（train 留給微調）。
用法：python eval_rooms_cc.py [--n-test 40] [--n-val 30] [--smoke N] [--thr 0.5]
輸出：eval_rooms/report.json、eval_rooms/chk/<id>_{gt,pred}.png、終端總表
"""
import argparse
import json
import os
import shutil
import sys
from dataclasses import replace
from xml.dom import minidom

import cv2
import numpy as np

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)),
                                "CubiCasa5k"))

DATA = "CubiCasa5k/data/cubicasa5k"
SUBSET = "high_quality_architectural"
IN_DIR = "eval_rooms/input"
CHK_DIR = "eval_rooms/chk"
REPORT = "eval_rooms/report.json"
CLASSES = ["kitchen", "living", "bed", "bath", "entry",
           "storage", "garage", "outdoor", "space"]


def norm_label(k):
    """管線房型 key → 評分 9 類：balcony 併 outdoor、room/None 併 space。"""
    if k in (None, "", "room"):
        return "space"
    return "outdoor" if k == "balcony" else k


def match_rooms(gt_masks, pred_masks, thr=0.5):
    """GT×預測 IoU 貪婪一對一配對，回傳 [(gi, pi, iou)]，iou≥thr。"""
    cand = []
    for gi, g in enumerate(gt_masks):
        ga = int(g.sum())
        for pi, p in enumerate(pred_masks):
            inter = int(np.logical_and(g, p).sum())
            if not inter:
                continue
            iou = inter / (ga + int(p.sum()) - inter)
            if iou >= thr:
                cand.append((iou, gi, pi))
    cand.sort(reverse=True)
    used_g, used_p, out = set(), set(), []
    for iou, gi, pi in cand:
        if gi in used_g or pi in used_p:
            continue
        used_g.add(gi)
        used_p.add(pi)
        out.append((gi, pi, iou))
    return out


def confusion(pairs):
    """[(gt_label, pred_label)] → 巢狀 dict 混淆矩陣。"""
    cm = {c: {c2: 0 for c2 in CLASSES} for c in CLASSES}
    for g, p in pairs:
        cm[g][p] += 1
    return cm
```

- [ ] **Step 2: 跑測試確認通過**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: 5 passed

- [ ] **Step 3: Commit**

```bash
git add tests/test_eval_rooms_cc.py eval_rooms_cc.py
git commit -m "test(eval): 房型評分純函式——IoU 貪婪配對/正規化/混淆矩陣（TDD RED→GREEN）"
```

### Task 3: 樣本挑選、GT 解析、管線調用、主流程

**Files:** Modify `eval_rooms_cc.py`（接續追加）、Modify `.gitignore`

- [ ] **Step 1: 樣本挑選＋GT 解析**

```python
def pick_samples(n_test=40, n_val=30):
    """val/test 清單中 SUBSET 的單層樓樣本（無 F2），排序取前 N。"""
    out = []
    for split, n in (("test", n_test), ("val", n_val)):
        with open(os.path.join(DATA, split + ".txt")) as f:
            ids = [ln.strip().strip("/").split("/")[1] for ln in f
                   if ln.strip().startswith("/" + SUBSET + "/")]
        elig = [i for i in sorted(ids, key=int)
                if os.path.isfile(os.path.join(DATA, SUBSET, i, "F1_scaled.png"))
                and not os.path.exists(os.path.join(DATA, SUBSET, i,
                                                    "F2_original.png"))]
        out += [(split, i) for i in elig[:n]]
        print(f"{split}: 合格 {len(elig)} 張，取 {min(n, len(elig))}")
    return out


def parse_gt(svg_path, h, w):
    """model.svg Space 多邊形 → [(9類label, bool mask)]；對位不符回 None。"""
    from floortrans.loaders.house import rooms_selected
    from floortrans.loaders.svg_utils import get_polygon
    import floorplan2room as f2r
    doc = minidom.parse(svg_path)
    svg = doc.getElementsByTagName("svg")[0]
    def _num(a):
        v = svg.getAttribute(a).replace("px", "")
        return int(round(float(v))) if v else 0
    if (_num("width"), _num("height")) != (w, h):
        return None                              # SVG 座標系與圖不符，跳過
    gt = []
    for e in doc.getElementsByTagName("g"):
        cls = e.getAttribute("class").split(" ")
        if not cls or cls[0] != "Space":
            continue
        cid = rooms_selected.get(cls[1] if len(cls) > 1 else "Undefined", 11)
        if cid in (0, 2, 8):                     # 背景/牆/欄杆不是房間
            continue
        rr, cc = get_polygon(e)
        m = np.zeros((h, w), bool)
        m[np.clip(rr, 0, h - 1), np.clip(cc, 0, w - 1)] = True
        if m.sum() < 100:                        # 退化多邊形
            continue
        gt.append((norm_label(f2r.CC_ROOM_LABEL.get(cid)), m))
    return gt
```

- [ ] **Step 2: 管線調用＋單張評分＋疊圖**

```python
def run_pipeline(img_path, cfg_bw, cfg_color):
    """floorplan2room 內部呼叫鏈（同 process()，但不落 json、回傳記憶體物件）。"""
    import floorplan2room as f2r
    is_color, _ = f2r.probe_color(img_path)
    if is_color:
        det = f2r.detect_color(replace(cfg_color, input=img_path,
                                       output="", preview=None))
    else:
        det = f2r.detect_bw(replace(cfg_bw, input=img_path,
                                    output="", preview=None))
    f2r.refine_scale(det)
    det["cc_file"] = f2r._cc_path(img_path)
    det["symbols"] = f2r.detect_symbols(det)
    labels, rooms, _b, _z, _e = f2r.build_rooms(det)
    return det, labels, rooms


def _overlay(bgr, masks_labels, path):
    """房型色塊半透明疊圖（GT 與預測共用；色表沿用管線）。"""
    import floorplan2room as f2r
    vis, fill = bgr.copy(), bgr.copy()
    for lab, m in masks_labels:
        fill[m] = f2r.ROOM_BGR_EX.get(lab, (150, 150, 150))
    vis = cv2.addWeighted(fill, 0.5, vis, 0.5, 0)
    cv2.imwrite(path, vis)


def eval_one(sid, cfg_bw, cfg_color, thr):
    """單樣本：GT 解析＋管線＋配對。回傳評分 dict（error/分割失敗亦結構化）。"""
    img = os.path.join(IN_DIR, sid + ".png")
    svg = os.path.join(DATA, SUBSET, sid, "model.svg")
    bgr = cv2.imread(img)
    h, w = bgr.shape[:2]
    gt = parse_gt(svg, h, w)
    if gt is None:
        return {"id": sid, "status": "svg_mismatch"}
    if not gt:
        return {"id": sid, "status": "no_gt_rooms"}
    det, labels, rooms = run_pipeline(img, cfg_bw, cfg_color)
    if labels is None or not rooms:
        _overlay(bgr, [(g, m) for g, m in gt],
                 os.path.join(CHK_DIR, sid + "_gt.png"))
        return {"id": sid, "status": "seg_fail", "n_gt": len(gt)}
    if labels.shape != (h, w):                   # 彩圖管線可能 2 倍
        labels = cv2.resize(labels.astype(np.int32), (w, h),
                            interpolation=cv2.INTER_NEAREST)
    preds = [(norm_label(r["label"]), labels == r["id"]) for r in rooms]
    pairs = match_rooms([m for _, m in gt], [m for _, m in preds], thr)
    matched = [(gt[gi][0], preds[pi][0]) for gi, pi, _ in pairs]
    _overlay(bgr, gt, os.path.join(CHK_DIR, sid + "_gt.png"))
    _overlay(bgr, preds, os.path.join(CHK_DIR, sid + "_pred.png"))
    return {"id": sid, "status": "ok", "n_gt": len(gt), "n_pred": len(preds),
            "n_match": len(pairs),
            "mean_iou": round(float(np.mean([i for _, _, i in pairs])), 4)
                        if pairs else 0.0,
            "pairs": matched,
            "ious": [round(float(i), 4) for _, _, i in pairs]}
```

- [ ] **Step 3: main（staging、快取、彙總、報表）**

```python
def main():
    ap = argparse.ArgumentParser(description=__doc__.split("\n")[0])
    ap.add_argument("--n-test", type=int, default=40)
    ap.add_argument("--n-val", type=int, default=30)
    ap.add_argument("--smoke", type=int, default=0,
                    help="只跑前 N 張（煙霧測試）")
    ap.add_argument("--thr", type=float, default=0.5)
    a = ap.parse_args()

    import floorplan2room as f2r
    import floorplan2dxf as fp_bw
    import floorplan2dxf_color as fp_c
    cfg_bw = fp_bw.load_config("config.ini")
    cfg_color = fp_c.load_config("config_color.ini")

    samples = pick_samples(a.n_test, a.n_val)
    if a.smoke:
        samples = samples[:a.smoke]
    os.makedirs(IN_DIR, exist_ok=True)
    os.makedirs(CHK_DIR, exist_ok=True)
    staged = []
    for _, sid in samples:
        dst = os.path.join(IN_DIR, sid + ".png")
        if not os.path.isfile(dst):
            shutil.copy(os.path.join(DATA, SUBSET, sid, "F1_scaled.png"), dst)
        staged.append(dst)
    f2r.ensure_cc_masks(staged)                  # 缺快取自動補（CPU ~1 分/張）

    results = []
    for n, (split, sid) in enumerate(samples, 1):
        print(f"[{n}/{len(samples)}] {split}/{sid}", end=" ", flush=True)
        try:
            r = eval_one(sid, cfg_bw, cfg_color, a.thr)
        except Exception as e:                   # 單張爆炸不拖垮批次，總表可追溯
            r = {"id": sid, "status": "error", "error": repr(e)}
        r["split"] = split
        results.append(r)
        print(r["status"],
              f'IoU {r.get("mean_iou", 0):.2f} 配對 {r.get("n_match", 0)}'
              f'/{r.get("n_gt", "?")}' if r["status"] == "ok" else "")

    ok = [r for r in results if r["status"] == "ok"]
    all_pairs = [tuple(p) for r in ok for p in r["pairs"]]
    all_ious = [i for r in ok for i in r["ious"]]
    n_gt = sum(r["n_gt"] for r in ok)
    n_pred = sum(r["n_pred"] for r in ok)
    cm = confusion(all_pairs)
    summary = {
        "n_images": len(results), "n_ok": len(ok),
        "n_seg_fail": sum(r["status"] == "seg_fail" for r in results),
        "n_skip": sum(r["status"] in ("svg_mismatch", "no_gt_rooms", "error")
                      for r in results),
        "gt_rooms": n_gt, "pred_rooms": n_pred, "matched": len(all_pairs),
        "hit_rate": round(len(all_pairs) / n_gt, 4) if n_gt else 0,
        "overseg": round(n_pred / n_gt, 4) if n_gt else 0,
        "mean_iou": round(float(np.mean(all_ious)), 4) if all_ious else 0,
        "confusion": cm, "iou_thr": a.thr,
    }
    per_cls = {}
    for c in CLASSES:
        tp = cm[c][c]
        gt_c = sum(cm[c].values())
        pred_c = sum(cm[g][c] for g in CLASSES)
        per_cls[c] = {"gt": gt_c, "precision": round(tp / pred_c, 3) if pred_c else None,
                      "recall": round(tp / gt_c, 3) if gt_c else None}
    summary["per_class"] = per_cls

    with open(REPORT, "w") as f:
        json.dump({"summary": summary, "images": results}, f,
                  ensure_ascii=False, indent=1)

    print(f"\n{'='*62}\n樣本 {len(results)} 張：評分 {len(ok)}、"
          f"分割失敗 {summary['n_seg_fail']}、跳過 {summary['n_skip']}")
    print(f"GT 房間 {n_gt}  預測 {n_pred}（過切率 {summary['overseg']:.2f}）"
          f"  命中 {len(all_pairs)}（{summary['hit_rate']:.1%}）"
          f"  配對平均 IoU {summary['mean_iou']:.3f}")
    hdr = "GT\\預測".ljust(8) + "".join(c[:6].rjust(8) for c in CLASSES)
    print("\n房型混淆矩陣（列=GT、欄=預測，僅 IoU≥%.1f 配對）:" % a.thr)
    print(hdr)
    for g in CLASSES:
        if sum(cm[g].values()) == 0:
            continue
        print(g.ljust(8) + "".join(str(cm[g][p]).rjust(8) for p in CLASSES))
    print("\n逐類  P / R:")
    for c in CLASSES:
        s = per_cls[c]
        if s["gt"] or s["precision"] is not None:
            print(f"  {c:8s} P={s['precision']}  R={s['recall']}  (GT {s['gt']})")
    print(f"\n報表 → {REPORT}   疊圖 → {CHK_DIR}/")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: .gitignore 追加**

```
# 房型評分：資料集圖複本（來源在 CubiCasa5k/data/，不重複進版控）
eval_rooms/input/
```

- [ ] **Step 5: 測試仍綠**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: 5 passed

### Task 4: 煙霧測試（3 張，肉眼驗證對位）

- [ ] **Step 1:** Run: `.venv/bin/python eval_rooms_cc.py --smoke 3`
Expected: 3 張推論＋評分完成、`eval_rooms/chk/` 出現 `_gt.png`/`_pred.png`
- [ ] **Step 2:** Read 疊圖確認 GT 色塊與圖面房間對位（無錯位/鏡像/縮放）
- [ ] **Step 3:** 驗證混淆矩陣行列和 = 配對數；有問題回 Task 3 修

### Task 5: 全量 70 張

- [ ] **Step 1:** Run: `.venv/bin/python eval_rooms_cc.py`（背景，語意推論約 70 分）
- [ ] **Step 2:** 檢查 report.json 與終端總表一致；抽查 3~5 張疊圖
- [ ] **Step 3: Commit**

```bash
git add eval_rooms_cc.py tests/ .gitignore
git commit -m "feat(eval): 路線圖 A 房型答案集評分工具 eval_rooms_cc.py"
```

### Task 6: 成績記錄

- [ ] **Step 1:** README 換機交接章節補推論/評分依賴（torch CPU/lmdb/scikit-image/svgpathtools）
- [ ] **Step 2:** README 路線圖 A 條目補上首輪基線成績摘要
- [ ] **Step 3:** Commit（docs）

---

## Plan Self-Review

- Spec 覆蓋：樣本挑選 ✓（Task 3 pick_samples）、GT 解析 ✓（parse_gt）、快取撞名 ✓（staging）、指標 ✓（match/confusion/summary）、疊圖 ✓、report.json ✓、錯誤處理 ✓（eval_one 結構化 status）
- 型別一致：match_rooms 回傳 (gi,pi,iou) 與 eval_one 使用一致；confusion 輸入 [(g,p)] 與 pairs 結構一致
- 無占位符
