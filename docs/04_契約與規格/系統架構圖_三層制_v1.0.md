# RoomPilot 系統架構圖(三層制)v1.0

> **狀態**:2026-07-18 組長核可入庫。草稿歷程 v0.1–v0.9 於 7/17–18 與組長逐輪審定,含三視角審查(流程覆蓋/repo 對照/資料流閉環)15 項修正全數落地。
> **前提**:圖 C 系列依 7/16 新五階段流程繪製,該流程**仍待 Sunny 老師批准**——若流程翻案,C 系列隨之修訂,圖 B 模組總覽大體不受影響。
> **三層怎麼讀**:圖 A=Demo/簡報版(組員與評審看這張,現場口語補充);圖 B=模組總覽(框、職責、主資料流,含實作三態標記);圖 C1–C5=分站步驟流,「不省線」的承諾由分站圖兌現。
> **畫法約定**:分站圖一律步驟流——一格=一步、由上往下、判斷用菱形,模組歸屬寫在步驟文字裡;模組之間的正式連線以圖 B 為準。單位契約:全鏈公分,例外處逐一標 ⚠。

---

## 圖 A · Demo 簡報版

```mermaid
flowchart TB
    subgraph FLOW["使用者走的路——五站,一路向前"]
        direction LR
        A["① 上傳＋辨識<br/>圖檔 → 房間格局<br/>拉比例、標房型"] --> B["② 問卷<br/>勾選＋自由文字<br/>特殊需求→LLM→JSON"]
        B --> C["③ 2D 佈局<br/>Agent 選家具<br/>引擎算位置"]
        C --> D["④ 3D 白模＋取景<br/>確認壓迫感<br/>鎖定一個視角"]
        D --> E["⑤ 渲染<br/>白模＋色卡<br/>→ 寫實提案圖"]
    end
    FLOW --> BASE["三個共同底座:server(唯一後端,所有 API 掛這)· Scene JSON(唯一資料源,公分)· 狀態機(記住你改了什麼、哪些要重做——規劃中)"]
```

## 圖 B · 模組總覽

實作三態:✅=已接線有測試 🟡=部分存在(框內註明現況住哪) ⭕=純規劃。前端與 server 的往來一律走 API(唯一入口)。

```mermaid
flowchart TB
    subgraph FE["前端層(React)"]
        FEUP["① 上傳平面圖 🟡"]
        FE2D["①③ 2D 編輯器 ⭕ 無主<br/>拉線比例 · 房型 · 節點切割 · 家具拖曳/旋轉"]
        FEQ["② 問卷(雙層)⭕"]
        FE3D["④ 3D 白模＋取景(R3F)🟡<br/>取景鎖定未做"]
        FEP["⑤ 提案畫面＋方案畫廊 ⭕"]
    end
    subgraph SV["服務層"]
        SRV["server(FastAPI)✅<br/>API 編排 · 座標系轉換唯一落點"]
        SM["狀態機(版本計數器)⭕<br/>失效規則 v0.1 的實作載體"]
        SJ[("Scene JSON 🟡<br/>單一資料源 · 公分 · version 欄位待加")]
    end
    subgraph ALGO["演算法模組(Python)"]
        FP["floorplan 辨識 ✅"]
        U3["upgrade3d ✅"]
        NLU["LLM 特殊需求解析 🟡<br/>現況在 server intake,獨立化=P0"]
        AG["agent 提示鏈 🟡<br/>語意提示＋recovery ✅;選件現在 server,搬入=P0"]
        EN["engine(Shapely)✅<br/>★ 家具座標唯一權威"]
        CAT["catalog/style_db ✅<br/>runtime=JSON 直讀"]
    end
    subgraph DATA["資料層"]
        DB[("PostgreSQL 🟡<br/>projects 表＋渲染履歷=規劃")]
        STYLE["風格資料 ✅<br/>6 風格 × 3 色系=18 色卡已入庫(server/static/style_cards)<br/>schema=taiwan_style_cards.json v2.0(card_id＋palette_hex×3)"]
        GLB["GLB 模型庫 ✅<br/>1,808 檔＋串流路由"]
    end
    RAPI["☁ 外部渲染 API ⭕ 待選型"]

    FE <--> |"上行操作 ⇄ 頁面資料＋失效旗標＋渲染 gate(見 C1–C5)"| SRV
    SRV -- "圖檔" --> FP
    FP -- "DXF(WALL/WINDOW)" --> U3
    FP -. "交接 JSON:門段＋比例信心度 ⚠ 第三套座標系,SRV 統一吸收" .-> SRV
    U3 -- "樓面 JSON ⚠ 現況公尺,待遷移" --> SRV
    SRV <--> |"特殊需求文字 ⇄ 結構化 JSON"| NLU
    SRV -- "需求 JSON＋空間屬性(公分)" --> AG
    CAT -- "候選家具(公分)" --> AG
    AG <--> |"提示 ⟲ 失敗回饋(recovery 迴圈)"| EN
    CAT -- "已選件尺寸＋淨空參數" --> EN
    EN -- "擺位結果(引擎座標系,SRV 轉換)" --> SRV
    GLB -- "家具模型串流" --> SRV
    STYLE -- "色卡清單" --> SRV
    SRV <--> |"截圖＋色卡 ⇄ 寫實渲染圖"| RAPI
    SRV <--> |"全場景讀寫(公分)"| SJ
    SRV <--> |"專案存檔 ⇄ 渲染履歷查詢"| DB
    CAT -. "離線匯入(scripts 管線)" .-> DB
    SRV -- "事件＋版本寫入" --> SM
    SM -- "失效判定＋渲染 gate" --> SRV
```

## 代號速查(S=狀態、E=事件;正本=《新五階段_狀態失效規則_v0.1.md》)

**S(State,狀態)**=使用者一路確認下來的六個成果,各自帶版本號;「version＋1」=這個狀態改了一版,下游拿版本號一比就知道自己過期沒。**E(Event,事件)**=使用者的七種「回頭改」動作,每個事件觸發一組失效規則。

- **S1** 比例確認 **S2** 房型確認 **S3** 2D 佈局 **S4** 白模 **S5** 取景(鎖定的視角) **S6** 渲染圖
- **E0** 換平面圖 **E1** 重拉比例 **E2** 改房型/節點切割 **E3** 改問卷答案 **E4** 家具增刪移動 **E5** 重新取景 **E6** 換色卡(唯一不失效的事件,產生平行變體)

## 圖 C1 · 站①:上傳＋辨識＋比例＋房型(S1/S2)

```mermaid
flowchart TB
    A["1. 使用者在上傳頁交圖檔 → server(PNG/JPG/DXF)"]
    F{"2. 檔案格式?"}
    P["3a. PNG/JPG → floorplan 辨識:<br/>產出 DXF(WALL/WINDOW)＋交接 JSON 回 server<br/>(門段＋比例信心度;⚠ 第三套座標系:<br/>px＋cm、原點左下——由 server 統一吸收)"]
    Dx["3b. DXF 直傳(跳過辨識)"]
    U["4. upgrade3d:DXF → 樓面 JSON 回 server<br/>⚠ 現況公尺,待遷移"]
    S5["5. server → 2D 編輯器:<br/>樓面幾何＋門窗(px,比例未定)"]
    S6["6. 使用者在 2D 編輯器:拉線兩點(px)＋輸入實距(公分)<br/>房型下拉修正 · 節點切割"]
    S7["7. server 換算並寫入 Scene JSON:<br/>校正後幾何(公分),S1/S2 version＋1"]
    S8["8. 狀態機:E1/E2 失效判定"]
    S9["9. server → 2D 編輯器:確認後幾何(公分)回顯"]
    A --> F
    F -- "PNG/JPG" --> P --> U
    F -- "DXF" --> Dx --> U
    U --> S5 --> S6 --> S7 --> S8 --> S9
```

## 圖 C2 · 站②:需求問卷＋特殊需求 LLM(輸入事件,非狀態)

```mermaid
flowchart TB
    A["1. server → 問卷頁:問卷題目(基礎/進階雙層)"]
    B["2. 使用者作答 → server:問卷答案＋特殊需求文字"]
    C["3. server 把特殊需求(自由文字)交給 LLM 解析<br/>🟡 現況=server intake,獨立化=P0"]
    D["4. LLM 回傳特殊需求 JSON → server"]
    E["5. server 把需求 JSON 寫入 Scene JSON"]
    F{"6. 狀態機 E3 判定:<br/>S3 佈局已存在?"}
    G["首次填卷:直接進站③生成"]
    H["7. 彈提示:重新生成(手動調整會消失)<br/>/保留現有佈局"]
    I["8. 使用者二選一 → server<br/>選保留=視同重新確認,不留過期標籤(※d)"]
    A --> B --> C --> D --> E --> F
    F -- "不存在" --> G
    F -- "已存在" --> H --> I
```

## 圖 C3 · 站③:2D 佈局——Agent⟲引擎生成迴圈＋手動微調(S3)

```mermaid
flowchart TB
    A["1. server → agent:需求 JSON＋空間屬性<br/>(房型/節點切割/房間幾何,公分)<br/>2. catalog → agent:候選家具(公分)"]
    C["3. agent → engine:選件＋layout_intent 語意提示<br/>(只說誰當錨點、靠哪面牆、誰優先——不含任何座標數字)"]
    D["4. engine(★全系統唯一有權產生座標的地方):<br/>算座標＋碰撞/淨空檢查"]
    E{"5. 全部擺得下?"}
    R["6. placement.failed 清單(品項＋原因)回 agent<br/>recovery:換同型較小款(回查 catalog)/移除<br/>→ 交回 engine 重擺至收斂"]
    F["7. engine → server:擺位結果=實際座標(引擎座標系)<br/>server 座標轉換＋寫入 Scene JSON,S3 version＋1"]
    G["8. server → 2D 編輯器:佈局(公分)＋家具縮圖"]
    H["9. 使用者拖曳/旋轉 → server:落點(公分)<br/>※無縮放:尺寸=型錄實物"]
    I{"10. engine 單件 validate:<br/>落點合法?"}
    J["11a. 合法 → 寫入 Scene JSON"]
    K["11b. 違規 → 當場亮紅退回"]
    L["12. 狀態機 E4 判定:白模無聲重生<br/>取景軟失效 · 渲染鈕灰"]
    A --> C --> D --> E
    E -- "有放不下" --> R --> D
    E -- "OK" --> F --> G --> H --> I
    I -- "合法" --> J --> L
    I -- "違規" --> K --> H
```

## 圖 C4 · 站④:3D 白模＋取景鎖定(S4/S5)

```mermaid
flowchart TB
    A["1. server 從 Scene JSON 讀全場景(公分)"]
    B["2. server → 3D 頁:白模場景<br/>⚠ 牆窗段現況公尺(three.js 契約,server 轉換,遷移中)"]
    C["3. GLB 模型庫 → server → 3D 頁:家具模型串流<br/>(/api/furniture/:id/model)"]
    D["4. 使用者確認壓迫感、調攝影機"]
    E["5. 按鎖定視角 → server:相機姿態<br/>(位置/旋轉/FOV,公分)＋定格截圖"]
    F["6. server 寫入 Scene JSON:姿態,S5 version＋1<br/>※b 比例變更後姿態超界時,自動拉回室內最近合法點"]
    G{"7. 上游有變動<br/>(狀態機判 S5 軟失效)?"}
    H["8a. 有:3D 頁顯示「姿態保留＋需重新確認」<br/>渲染鈕灰,重按鎖定後恢復"]
    I["8b. 無:渲染 gate 通過,可進站⑤"]
    A --> B --> C --> D --> E --> F --> G
    G -- "軟失效" --> H --> D
    G -- "無" --> I
```

## 圖 C5 · 站⑤:色卡選擇＋渲染＋方案畫廊(S6)

```mermaid
flowchart TB
    A["1. server → 提案畫面:色卡清單<br/>(出自風格資料 6 風格 × 3 色系=18)＋ gate 狀態"]
    B["2. 使用者選定色卡(card_id)＋按渲染"]
    G1{"3. 狀態機 gate:<br/>S1–S5 全確認且非過期?"}
    G0["渲染鈕灰住,<br/>回對應站補確認"]
    Q{"4. 渲染履歷查詢(PostgreSQL):<br/>同(白模版本＋取景版本＋色卡)已有圖?"}
    HIT["5. 命中=快取:直接回存圖<br/>不重打 API、不花 5 分鐘"]
    MISS["6. 未命中:server 送定格截圖＋色卡參數<br/>→ ☁ 外部渲染 API(待選型)"]
    R7["7. 寫實渲染圖(＜5 分鐘)回 server"]
    W8["8. 圖＋渲染履歷寫入 DB<br/>(白模版本＋取景版本＋色卡)"]
    FIN["9. 提案畫面:渲染圖＋血統<br/>過期圖標「歷史方案」陳列<br/>E6 換色卡=平行變體並列"]
    A --> B --> G1
    G1 -- "未過" --> G0
    G1 -- "通過" --> Q
    Q -- "命中" --> HIT --> FIN
    Q -- "未命中" --> MISS --> R7 --> W8 --> FIN
```

---

## 繪製決策(7/17–18 組長裁決紀錄)

- **三層制**(7/18):圖 A 簡報/圖 B 模組總覽/圖 C1–C5 分站步驟流;「不省線」由分站圖兌現,圖 B 維持可讀密度。
- **兩份圖制→三層制演進**(7/17→7/18):先裁定 Demo 簡版+內部詳版兩份;審查發現單張詳版補全 15 項缺漏會爆炸,升級為三層。
- **實作三態標記**:✅/🟡/⭕ 統一標在框上,純規劃不再畫得像已存在(狀態機、projects 表、⑤站前端均為 ⭕)。
- **問卷一律過 server 轉手**(7/17),不直連 agent——維持唯一 FastAPI/唯一入口。
- **特殊需求 LLM→JSON 外顯為獨立框**(7/17)——履歷亮點,明著畫。
- **Agent 能力清單不進圖**(7/17),另立文件:`docs/02_復刻研究/Agent能力清單_v0.1.md`。
- **風格定案**(7/18):6 風格(北歐/日式/現代簡約/奶油風/工業風/美式)× 3 色系=18 張色卡,已全套入庫並接線;剩餘工作=palette_hex→渲染 API 參數映射。
- **砍掉:帳號層**——存專案用 projects 表即可;SaaS 預留=auth middleware+owner_id 一句話。
- **砍掉:Redis**——快取=渲染履歷本身(C5 第 5 步)。
- **P1 預留**:RAG 選件檢索與自然語言微調,五站全綠後依序做,不進圖。
- **單位遷移工項(四處)**:① upgrade3d 樓面 JSON(公尺) ② three.js 牆窗段 payload(公尺) ③ agent/layout_intent 內部自行 ×100 ④ floorplan 交接 JSON 第三套座標系(原點左下、y 向上)——全部由 server 統一吸收,公尺與異座標系不外洩到其他模組介面。
- **誠實修正**:DB→型錄改標「離線匯入」(runtime 現況=JSON 直讀);AG 框註明選件現在 server、搬入=P0;FE2D 移除「縮放」(家具尺寸=型錄實物,要更小走 recovery 換款)。
- **規格文件不填人名**(7/18):分工凍結,等架構圖、介面契約、狀態圖、時序圖、WBS+甘特出齊後再整理。

## 審查 15 條 → 修正落點(7/18,三視角審查)

1. ③ 微調上行＋validate 迴路缺 → C3 第 9–12 步。
2. ⑤ 站前端不存在＋DB 無讀取線 → FEP 框＋C5 整張。
3. 站① 迴圈斷＋Scene JSON 牆窗無來源 → C1 第 5、7 步。
4. 狀態機只進不出 → 圖 B 雙向標籤線＋各站 gate 下行。
5. 門資料鏈(交接 JSON)缺 → C1 第 3b 步＋遷移工項④。
6. Agent 選件矛盾群 → C3 第 1–6 步全迴圈＋AG 框三態註記。
7. 中低九條(姿態存讀、E3 彈窗、色卡入口、GLB 線、DB→CAT 方向、三態標記、縮放、px 標籤、圖 A 加 LLM 行)→ 各分站圖與註記逐一落地。

---

同進退文件:《新五階段_狀態失效規則_v0.1.md》《新五階段_測試檢核表_v0.1.md》《Agent能力清單_v0.1.md》(現於 docs/02_復刻研究/,Sunny 批准後升格本資料夾)。
